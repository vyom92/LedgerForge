# Salary and current AlDar

<a id="packet-al-dar-planner-reference-packet"></a>

**Owners:** bounded [FW-P3-08](../FUTURE_WORK.MD#fw-p3-08), separate [FW-P3-19](../FUTURE_WORK.MD#fw-p3-19).

## Current decision — 2026-09-15

Sprint 94 and the final owner corrections are **ACCEPTED** under `SPRINT_94_CURRENT_AL_DAR_AND_THIS_MONTH_PLANNING_ACCEPTED`. The [accepted outcome](../Archive/Accepted%20outcomes/Sprints_90-99.md#sprint-94) owns closure, exact evidence and limitations. The final two-decimal presentation correction below has one focused passing test, fresh Debug/optimized Release and native proof. The preceding 567-definition / 627-execution ordinary result is retained at its own fingerprint and was explicitly not rerun for this presentation-only delta. The owner's earlier “verified all” covers the prior corrected app; the final presentation-only delta is now also accepted by Chat. Closure publishes the unchanged product/test bytes with no new verification campaign. Remaining approximately five-second Transactions and two-second other-tab delays stay an OPEN defect in the selected post-94/pre-95 correction handoff. [ADR-045](../ADR.md#adr-045) owns the planner contract; [ADR-047](../ADR.md#adr-047) owns the V17→V18 backup bridge. The accepted predecessor is Sprint 93 at `main@99f27b376fe6f3a37e6a15f7cd65c5e16a49caed`.

The owner authorized personal automated access to the public Al Dar reference. No provider-inquiry or terms approval gate remains for this selected private workflow. This records owner authorization, not a separate provider permission statement or guaranteed settlement.

## Final two-decimal presentation correction — 2026-09-15

The current product is a **QAR-1 current Al Dar unit planning reference**, indicative and used to estimate a transfer. The UI now uses the existing presentation-rate property instead of the raw provider token and displays exactly two decimal places, including trailing zeros. Decimal rounding is confined to that display property. The raw token, exact-ratio calculation, applied evidence, persistence, provider request, V18 and Dashboard source are unchanged by this delta. Historical amount-specific evidence remains readable; it does not define the current unit-reference workflow.

Final fingerprint: **334 source/test/build/resource inputs**, SHA-256 `4f262c7d8ba35110d9aa77aec162a0d951723c93a57d62455a8c36cde15d1986`.

| Final bounded execution | Result |
| --- | --- |
| `AlDarReferenceTests.testUnitRateDisplayUsesTwoDecimalsWithoutChangingExactRatio` | **1 definition / 1 execution / 0 failures / 0 skips**; `LedgerForge-validation.CVNCeX/TestResults.xcresult`. Covers rounding, whole/fractional trailing zeros, unchanged raw evidence and a case where feeding the rounded display back into the exact calculation would produce different Money. |
| Debug | PASS; `LedgerForge-validation.5UxLZQ`. |
| Optimized Release | PASS; `LedgerForge-validation.PbrqL8`. Release was not opened against the owner's separate Release database. |
| Quick native check | PASS in the corrected Debug app: **1 QAR = 26.25 INR**, with **Fetched 15 Sep 2026 at 20:25 · UTC+03:00**. One explicit QAR-1 Refresh used the unchanged client; the result remained pending/not applied. No Apply, Save, planner edit, resize or appearance change. |
| Saved-state containment | The in-memory logical comparison of all **54 tables** and preference-file bytes remained equal before/after this corrected restart/native check. No financial comparison file or screenshot was written to disk. |
| Ordinary app regression | **NOT RERUN by explicit owner instruction**. The prior 567/627 result and its exact fingerprint remain below; this table does not claim an ordinary run against the last display-only bytes. |

Current documentation removes Dashboard Planning FX as a present/future card and keeps the new shared current-market QAR/INR/USD component under Sprint 99. The settled roadmap now assigns Budget Planner to 95, identity to 96, holdings then valuation to 97, integrated acceptance to 98 and market FX/net worth to 99; accepted 95 is a hard prerequisite for 100. The [post-94 corrections](../FUTURE_WORK.MD#post-94-pre-95-corrections) must precede 95. This bounded execution originally stopped for acceptance without publishing. The subsequent owner/coordinator acceptance now authorizes documentation closure and exact-path publication only; no additional product scope, migration or verification campaign is included.

## Selected implementation

- Bounded existing Salary panel and manual rate/date fields; explicit Refresh Al Dar → pending candidate → Use Reference → draft → existing Save/Command-S. No auto-apply, startup fetch, timer or retry.
- One forward public `POST /aldarportal/Home/GetRate`: INR, exact positive QAR decimal string, `isFCY=false`. The ephemeral client retains no cookies, credentials or cache, checks normal HTTPS/final endpoint/status/JSON MIME and bounded body, and refuses redirects.
- Raw positive plain JSON receive decimal is retained separately from Money, bounded to 32 coefficient digits / 28 fractional digits. Exact integer ratio arithmetic rounds only at the final QAR cent; the UI displays exactly two decimals without changing that durable authority.
- Optional applied external evidence and unchanged manual FX are mutually exclusive. Additive V18 creates only the child table and exclusion triggers; no parent rebuild or V1–V17 edits. Shared checks cover lexical/Money identities, exclusivity and INR shortfall on save and external-reference staged hydration.
- The final owner decision always looks up QAR 1, independent of manual FX, balances, field validity or positive shortfall; it supersedes the former request-seed order. The quote remains temporary until explicit Use binds it to the current positive INR shortfall. External authority never rolls forward; an applied reference is invalidated when its bound shortfall changes.
- One explicit application calculation preserves the submitted QAR 1 and raw receive token. Fetched time includes this Mac's UTC offset. Saved evidence describes what the saved plan used, with no invented provider TTL/time/fee/ID or guaranteed transfer rate. The final coordinator correction removes all Dashboard planning-FX presentation; manual fallback and Al Dar details stay in the monthly planner. General market FX remains future-only.
- Format 1 stays. New backups use V18; direct V18 restore and exactly V17 isolated upgrade are supported. Original V17 package bytes stay unchanged. Its original payload SHA remains receipt identity; the prepared V18 candidate has a separate in-memory hash. V16/earlier/future/unknown histories reject. Retained V17 receipt startup may verify, apply V18, hydrate and complete the receipt.

## Prior bounded verification — 2026-09-15

**PRE-DELTA CANDIDATE / NOT YET PRODUCT-ACCEPTED.** The bounded checks passed against the prior candidate. Its Debug and optimized Release evidence predates the final completion delta and is not proof of the later bytes. Local build/test artifacts are outside the repository in the task-owned `LedgerForge-validation.kjzZ4U` temporary directory. Counts below are executions of the named selections, not additive claims of unique coverage.

| Execution | Result and exact scope |
| --- | --- |
| Initial focused selection | 44/44 passed: 9 AlDarReferenceTests, 21 BackupPackageTests and 14 recorded PlannerEditor tests |
| Post-review persistence/recovery selection | 30/30 passed: the 9 Al Dar and 21 backup/package checks after the durable shortfall-binding hydration correction |
| Applied-reference product backup check | 1/1 passed: a V18 product backup and isolated verification preserve the external child row from ordinary manual planner inputs |
| Final Al Dar selection | 11/11 passed, including the applied-reference backup check and the added repeated-Refresh/cancellation ownership check |
| Production provider client | 1/1 passed using one authorized non-owner sample amount; **1 of 1 additional live requests consumed** |
| Final builds | Debug and optimized Release passed; no broad TestPlan was invoked |

The focused coverage includes raw token bounds/precision, exact-ratio ceiling and overflow, pending/apply/save separation, manual fallback, dual-authority exclusion, persistence/reopen, invalid durable INR binding rejection before hydration, binding changes, unrelated edits, rollover seed-only behavior, failed/late fetches and cancellation. These use source-independent mechanics and ordinary manual planning inputs; they are not fabricated statement evidence.

The genuine recovery drill used the two retained Sprint-93 V17 product backups only in operation-owned isolated app namespaces. Product backup/restore and ordinary startup performed the work; the existing separate in-memory SQLite comparison checked logical equality. No financial projection was written to disk.

| Genuine database/recovery boundary | Observed result |
| --- | --- |
| Retained populated V17 package → isolated V18 restore | Original verification, separate candidate upgrade, activation and canonical hydration passed. All pre-existing ledger rows, old schema objects, other SQLite sequences and relevant metadata remained equal; only V18 history/schema were added, with an empty external-reference child. Clean relaunch matched all 54 tables exactly. |
| V18 product creation and direct restore | The product created a format-1 V18 package; manifest, hash and payload comparison passed. Direct restore into a separate isolated namespace and its clean relaunch both matched the verified restored projection exactly. |
| V17 canonical database with retained Sprint-93 receipt | Startup verified/migrated to V18, hydrated and completed the receipt; the next clean relaunch matched all 54 tables. |
| Failure after candidate open/hydration, before activation decision | Product rollback succeeded, preserved the prior valid ledger and reopened it on relaunch with exact equality. |
| Process interruption before durable activation decision | The injected exit occurred after preservation. Restart recovered the prior ledger with exact equality. |
| Process interruption after durable activation decision | Restart retained and hydrated the restored ledger with exact equality. Original backup/receipt identity was preserved. |
| Actual Current Database, after the isolated gate passed | The old app quit cleanly. Ordinary Debug startup selected Current Database, migrated V17→V18 and completed canonical hydration. All prior ledger rows/schema objects remained exact with only the approved additive delta. A second clean startup selected the same canonical database and matched all 54 tables: zero missing, extra or changed records/tables. The retained receipt identity remained equal and its phase was `relaunchConfirmed`. |

Both original V17 backup packages and saved appearance/profile preferences compared unchanged after the full drill and actual Current Database relaunch. The receipt payload SHA continues to identify the original restored backup, not mutable live SQLite bytes. V1–V17 migration source remained byte-identical to the accepted baseline.

Native Salary / This Month was inspected at the existing unresized window after Current Database migration and again after relaunch. The reference controls fit the existing transfer-planning panel; an absent India shortfall correctly disables Refresh and explains why. No planner values, saved appearance or profile settings were changed. Pending/applied-reference native presentation, a real Salary-plan save and a personal remittance quote were **NOT EXECUTED**; their source-independent state mechanics are covered above. The app was left open to Salary for owner review.

Implementation corrections and non-passes are preserved: review found a missing durable shortfall-binding check before hydration, which was corrected and rerun; the asynchronous Objective-C redirect callback caused a compiler crash and was replaced by the synchronous callback with the same redirect refusal. One live-test invocation skipped before networking because of its reservation path, consuming no request. One isolated launch used an overlong namespace and failed before database open; the corrected bounded namespace succeeded. These initial attempts are not counted as successful proof.

No original statement was opened or processed, no real Salary plan was saved, and no financial comparison dump or provider response file was created. No further live sample is authorized. Broad regression was suspended for this earlier execution; the owner has since resumed ordinary app regression plan. A new parser/source-corpus campaign still requires a relevant diff gate. Hardware power loss, real disk-full/permission failures, interruption inside the per-file recovery loop, real cleanup failure and long genuine-copy cancellation were not newly exercised; Sprint-93 limits remain in its accepted record. Release was built but was not launched against the owner's separate Release database in this sprint.

## Monthly Budget / This Month direction — owner recording, 2026-09-15

The current Salary page is not the final monthly-planning UX. The intended **Monthly Budget / This Month Calculator** uses the existing **Budget Analysis workbook Dashboard workflow** as its primary workflow reference; Salary actuals/history are supporting inputs. The future review covers monthly available funds, Qatar obligations, India requirements, existing account balances, remittance requirement, investments/other monthly allocations and resulting Qatar buffer, with less manual micromanagement.

This need remains under existing FW-P3-08/09/25 planning authorities and associated FW-P3-26/34 inputs; FW-P3-19 salary-to-bank reconciliation stays separate. The owner now assigns the rework to **Sprint 95**, whose acceptance is a hard prerequisite for Sprint 100. Salary History stays as supporting evidence. No redesign is implemented in Sprint 94; Sprint-95 architecture and implementation still need their approved prompt. The [current FX note](Current_FX_and_net_worth.md) records future Sprint-99 reporting/refresh requirements, separate from the QAR-1 indicative Al Dar unit planning reference.

## Final completion delta — verified candidate, acceptance pending, 2026-09-15

**HISTORICAL CHECKPOINT:** superseded by the final owner corrections, presentation-only delta and accepted Sprint-94 outcome. The following original execution evidence and former publication hold are retained as history.

Owner authorization adds the presentation-only Dashboard FX card and one populated manual planningFX rollover regression under V18. Broad regression suspension is lifted. Final Debug, optimized Release, ordinary app regression plan, native Dashboard/Salary inspection and Git/source/privacy containment are required before reporting the final candidate ready. The owner separately authorizes separating incompatible corpus/prohibited-artifact/live tests from the ordinary plan while retaining their test definitions. Genuine-source in-memory app mechanics remain included. No additional live provider sample, personal plan save, market FX implementation or publication is authorized.

The new `testPopulatedManualFXRollsOverExactlyUnderV18` covers a populated manual Decimal rate, observation day and configured fee through canonical save, month rollover, save/reload and checked reopen. It also checks that neither saved nor pending Al Dar authority appears. `testDashboardFXReadsOnlyCanonicalCurrentMonthAuthority` covers saved manual authority, pending/applied draft isolation, saved external evidence, absent current-month authority and provider-generation invalidation. Both new definitions passed in the initial focused execution; final ordinary verification below supersedes that earlier byte boundary.

The first ordinary run selected 554 definitions / 614 executions: 547 definitions / 607 executions passed, seven failed, zero skipped. Four failures were stale V17 assumptions in current-chain/concurrent-migration checks or historical V17 tests that unintentionally ran V18. The historical tests now explicitly stop at V17; current-chain tests include V18. The remaining stale assertions expected the old Settings minimum, an obsolete Dashboard account-card/View-all section, and no prepared-statement presentation after completed import. They now assert the already-accepted Settings and Dashboard contracts and truthful completed import outcome. No accepted migration SQL/checksum or product import behavior was changed for these failures.

### Final-byte verification

The final Debug, optimized Release and ordinary app regression plan used the same 334 source/test/build/resource inputs: aggregate SHA-256 `e42d2bb77fcd3a63fc911d874b65ea1a38b58a15467a9631e1c373232def7049`. The fingerprint was rechecked after all three executions and native inspection. These bytes precede the final account-role/card-title corrections below; the owner retains this ordinary app regression result and requires bounded focused verification for those corrections. These executions include the final hydration correction, the populated manual rollover and saved-Dashboard-authority checks, the retained Dashboard layout correction and the seven corrected test definitions.

| Final execution / observation | Result and boundary |
| --- | --- |
| Debug build | PASS; task-owned artifact directory `LedgerForge-validation.mXofN4`. |
| Optimized Release build | PASS; artifact directory `LedgerForge-validation.YU376x`. Release was not opened against the separate owner Release database. |
| Ordinary app regression plan | **554 definitions / 614 executions PASS; 0 FAIL; 0 SKIP**. Artifact `LedgerForge-validation.Yx9uae/TestResults.xcresult`. Twelve parameterized definitions produced 72 cases, accounting for the extra 60 executions. |
| Exact ordinary selection | The executed identifiers equal all 554 enabled definitions, with zero missing/unexpected identifiers and zero excluded definitions executed. Seventeen definitions remain intact outside the ordinary selection: 13 corpus definitions, one evidence-output preflight, two copied-source checks and one live-client check. The [plan](../../TestPlan.xctestplan) owns the list; the [validation driver](../../script/validate.sh) forwards the same list to Xcode because plan-only Swift Testing exclusions were not honored in enumeration. This is the owner-approved ordinary selection, not the prior unfiltered target. |
| New focused regressions in the final plan | `testPopulatedManualFXRollsOverExactlyUnderV18` and `testDashboardFXReadsOnlyCanonicalCurrentMonthAuthority` both PASS. |
| Existing manual planner/editor checks | All 14 definitions in [PlannerEditorTests](../../LedgerForgeTests/PlannerEditorTests.swift) PASS, with 15 executions because the fee persistence check has two arguments; zero failures/skips. Coverage includes manual FX entry/validation, the Save path shared with Command-S, canonical reload, manual-only first-plan persistence/reopen, configured-fee validation/persistence, draft editing/undo, canonical conflicts, provider-generation rejection and no write replay after refresh/provider failures. The added rollover check closes the populated manual-FX gap; AlDarReferenceTests also verifies manual/external exclusion. |
| Native Dashboard | PASS at the existing 1710 × 1073 window: genuine INR and QAR bank/card positions are visible, Recorded Activity remains separated by native currency/domain, and the FX card truthfully reports the absent saved current-month rate. No missing-QAR defect was observed. |
| Native Salary / This Month | Ordinary/manual controls fit and are visible; absent India shortfall disables Refresh, the existing manual rate/date and configured-fee fields remain present, and Save is visible. No personal plan was entered or saved. Populated saved-manual, pending and applied Al Dar native states remain **NOT_OBSERVED**; their state mechanics are covered by the tests, not manufactured screenshots. |
| Current Database and containment | Read-only logical comparison across all 54 tables remains equal before/after the final checks. No personal financial write, extra live provider request, unrelated corpus campaign, source mutation or derived financial evidence file was produced. Debug and Release resource-containment checks passed. No appearance/profile control was changed; the full preferences-file byte hash is not claimed unchanged after the final native launch. |

That Debug product was left open for owner testing at the prior completion checkpoint. No staging, commit or push occurred. Local `main`, cached `origin/main` and fresh remote `main` remain at the accepted Sprint-93 baseline above; the complete Sprint-94 candidate is unstaged work. Passing builds/regression/native presentation do not certify the remaining tab-switch latency or growing-history performance.

## Final owner corrections — 2026-09-15

**PRIOR CHECKPOINT:** the final two-decimal section above supersedes this fingerprint and raw-rate display. The recorded 45/46 focused and 567/627 ordinary executions remain valid evidence for these preceding bytes.

The owner supersedes the previous Dashboard card recommendation and shortfall-dependent Al Dar lookup, and adds the following bounded changes:

- **Always-QAR-1 lookup:** Refresh is independent of Salary field completion and shortfall. A raw quote is temporary and visible with exact returned rate/fetch time. Explicit Use creates positive-shortfall-bound evidence; Save remains separate. Amount mismatch, stale generation, obsolete plan and cancellation still reject. No provider/endpoint, migration, automatic refresh or general FX service was added.
- **Planner-only FX:** the Dashboard FX card, planning-rate caption and unused rate projection are removed. Its ordinary saved Salary/Funding summary remains; manual/external draft changes do not affect it before Save.
- **Zero entry:** untouched scalar defaults and new commitment amounts appear blank with a 0 placeholder. Entered/saved/rolled/captured zeros remain visible; clearing a required entered value stays invalid and cannot silently save an old value.
- **Calendar presentation:** actual months use Aug 2026 format in Salary, history and the Dashboard summary. Selecting the manual observation-date field opens a native picker; opening/cancelling is not a draft edit. Only the selected manual calendar day enters the unchanged YYYY-MM-DD field.
- **Investment availability:** max(0, funds before investment), while the underlying signed funds-before-investment and final QAR buffer retain deficits. Missing inputs remain unavailable.

The final candidate fingerprint is **334 source/test/build/resource inputs**, SHA-256 `04879c91629c439f8eac09feba2c1e1ff947bca9474233692f2b2cbdb0e82e90`. Final focused execution passed **45 definitions / 46 executions / 0 failures / 0 skips**: AlDarReferenceTests 18/18, PlannerEditorTests 19/20 and SalaryParserAndPlannerTests 8/8. Artifact: `LedgerForge-validation.fLIohN/TestResults.xcresult`. Fresh Debug (`LedgerForge-validation.73IEXm`) and optimized Release (`LedgerForge-validation.Ghb8Gn`) passed. Two preceding focused executions passed 42/43 and 43/44 before the final Dashboard/month/date/arithmetic deltas; neither substitutes for the final result. The renewed **ordinary app regression plan passed 567 definitions / 627 executions / 0 failures / 0 skips** against that same final fingerprint, artifact `LedgerForge-validation.CRey7v/TestResults.xcresult`. Twelve parameterized definitions produced 72 runs, accounting for the extra 60 executions. All 567 observed definitions were unique; none matched the unchanged exclusion selectors. The 12 selectors still cover the same 17 retained separately gated definitions; no additional exclusion was introduced. This is not complete authentic/source acceptance.

The corrected Debug app opened the genuine Current Database. Native inspection confirmed Dashboard FX removal, Sep 2026 labels, existing genuine INR/QAR banks/cards, preserved saved values and a native observation-date picker that was opened and cancelled. One explicit **QAR 1** lookup through the product succeeded without a manual planning rate and remained visibly pending/not applied with a fetch timestamp. This later lookup was authorized by the owner's fixed-QAR-1 request and is separate from the earlier reserved production-client sample. No Apply, Save, account/amount/date edit, original-source processing, window resize or appearance change was performed for native verification. The complete 54-table logical projection and preference bytes compared unchanged against the post-owner-Save baseline. Fresh default-zero entry and negative availability/deficit boundaries were tested with ordinary manual inputs; no personal state was manufactured for screenshots. The app was left open with the pending quote for owner testing. The owner subsequently reported **“verified all”** for the corrected app; this records owner native verification, alongside the completed build/ordinary regression evidence; no publication authorization is inferred.

The future shared QAR/INR/USD market authority, two-decimal display with retained calculation precision, timestamp/status, immediate local conversion selection and existing refresh/stale policy are recorded in the [FX authority](Current_FX_and_net_worth.md). No market-FX implementation is included. Navigation latency remains unresolved; the proposal below is not implemented or newly scheduled. No stage/commit/push or automatic acceptance.

## Final product corrections — verified, acceptance pending, 2026-09-15

**PRIOR CORRECTION CHECKPOINT:** its card title and shortfall-only lookup are superseded by the final owner corrections above; its execution history remains unchanged.

The owner first requested removing the INR NRE/NRO naming heuristic and renaming the saved-planning Dashboard card. Subsequent explicit clarification supersedes the institution restriction too: **native QAR or INR; bank accounts for balances; credit cards for commitments; no NRE/NRO or institution-name condition**. The owner chose to link a card while keeping its payment amount manual. No statement amount is automatically captured or applied.

| Eligibility boundary | Before | Corrected |
| --- | --- | --- |
| Planning balances | Active bank with a non-nil repository ID; QAR only for CBQ, or INR only for Axis/HDFC plus NRE/NRO in display name/nickname | Active bank with a nonempty repository ID and native QAR/INR; institution and names do not affect membership |
| Commitment account choices | Same-currency eligible bank, labelled Funding account | Same-currency active credit card with a nonempty repository ID, labelled Credit card; amount remains manual |
| Previously saved bank references | Existing optional account link | Preserved exactly and shown as Saved funding bank; not cleared, changed into a card, or made a new bank-selection option |

The existing account FK and workspace/currency relationship checks already support the link; the calculator does not read it. No migration, account/source identity rewrite, amount capture or financial formula change was made. The existing runtime active-status check is preserved; this correction does not add durable archive/closed-account state. The Dashboard heading is now **Planning FX**, with every other byte of ContentView unchanged from the previously validated candidate. Reversing only the title replacement reproduced its prior file hash, proving the layout correction stayed frozen. Its saved-current-month behavior, unavailable text and absence of fetch controls remain unchanged.

The corrected source/test/build/resource fingerprint is 334 inputs, SHA-256 `49aa8fc800b7cab08368a563f39823ab16193d4bce171737267b3b0677d43878`, unchanged across final focused tests, Debug/Release builds and native checks. Product changes for this correction are confined to ContentView, SalaryWorkspaceViewModel and SalaryView; tests change only SalaryParserAndPlannerTests. Earlier Sprint-94 product work, TestPlan exclusions, migrations and the validation driver remain byte-identical to the pre-correction candidate.

| Corrected-candidate verification | Definitions / executions | Result |
| --- | --- | --- |
| SalaryParserAndPlannerTests | 8 / 8 | PASS |
| PlannerEditorTests | 14 / 15 | PASS; includes the two-argument fee test |
| AlDarReferenceTests.testDashboardFXReadsOnlyCanonicalCurrentMonthAuthority | 1 / 1 | PASS; injected source-independent reference, no live request |
| **Final focused total** | **23 / 24** | **0 failures, 0 skips**; `LedgerForge-validation.0xSqsH/TestResults.xcresult` |
| Debug build | — | PASS; `LedgerForge-validation.iG1ZD7` |
| Optimized Release build | — | PASS; `LedgerForge-validation.LPOZcT`; Release database not opened |

The three new definitions are `plannerEligibilityIgnoresDisplayNameAndNickname`, `plannerEligibilityUsesNativeCurrencyAndAccountRoleWithoutInstitutionRestrictions`, and `manualCardCommitmentLinkHydratesWithoutChangingAmountOrLegacyBankLink`. They verify rename-insensitive bank/card membership, institution independence, typed roles/currencies, inactive/absent/empty-identity exclusion, same-currency card choices, unchanged manual amount/calculation, canonical SQLite hydration and preserved bank references. They use account metadata and ordinary manual planning inputs in isolated memory; they contain no statement fixture or imported financial graph. Two earlier focused executions also passed before the final owner clarification/empty-ID refinement and are not substituted for this final-byte result.

Native verification used genuine Current Database accounts. **Axis Bank INR, HDFC Bank INR and Commercial Bank of Qatar QAR** appear under Balances to include, with cards absent. The INR commitment menu offers **Axis Bank Credit Card**; the QAR menu offers **American Express Credit Card** and **Commercial Bank of Qatar Credit Card**. Existing saved bank links are visible as Saved funding bank. Menus were opened and dismissed without selecting an account, changing amounts or saving. Dashboard visibly shows Planning FX with its unchanged unavailable text because the saved plan has no FX authority. No window resize or appearance edit was performed.

The owner saved the existing Salary draft and explicitly authorized restart. That owner-saved state became the pre-restart baseline; all 54 database tables and the preferences-file bytes compare equal after the corrected app reopened and after native inspection. The correction did not write Current Database, discard the owner's saved plan or alter saved links. The corrected Debug app remains open to Salary for testing.

The owner subsequently reported that Refresh Al Dar remained disabled after populating the fields. Read-only native inspection showed included INR liquidity covering the included India commitments, with no positive India funding shortfall and the existing “No India shortfall to quote” guidance. Source inspection confirmed the approved positive-shortfall/request-amount gate. This observation does not demonstrate a failed provider request: the current flow requires a positive shortfall and valid request seed before explicit Refresh. No field was changed, request made, plan saved or product correction introduced for this observation.

The earlier **ordinary app regression plan** result remains applicable under the owner's bounded-correction authorization: **554 definitions / 614 executions, zero failures/skips**, on its recorded earlier fingerprint. It was not rerun or represented as a run on the corrected fingerprint. The final focused result covers the changed boundary; no broader behavior boundary or unexplained regression was exposed. The same **17 identified definitions remain retained and separately gated** for source/corpus campaigns, prohibited financial-evidence file output, copied-statement replay and live provider access. The three new definitions are not excluded. No complete authentic/source acceptance, source corpus, live-provider campaign or additional performance optimization ran.

Debug and Release resource containment passed, as did source/whitespace/link checks. No statement, financial projection, credential, database, screenshot or build artifact entered Git. No staging, commit or push is authorized or performed. Product acceptance remains with the owner/coordinator.

### Populated Dashboard layout correction and growth observation

The owner reports multi-second tab transitions after importing roughly 20–25 statements. The owner identifies the database as disposable initial seeding for mixed account/currency testing and expects approximately five additional statements every month indefinitely. The reported correlation is not itself a diagnosis.

Native investigation reproduced initial Dashboard layout stalls with both the prior Xcode product and the first final-delta Debug product. Stack samples showed SwiftUI/AppKit layout and AttributeGraph invalidation, not database query work. Debug startup installs canonical stores before constructing ContentView, so the first layout already includes the populated native positions. Replacing the outer full-tree `ViewThatFits` reduced the stall but a controlled startup still required 36.608 seconds. Temporary omission of the position section reduced that to 2.488 seconds; omitting activity took 23.817 seconds, and omitting recent activity took 29.299 seconds. These are diagnostic isolation timings, not UI acceptance of omitted content.

The retained correction chooses the existing Dashboard and Bank/Card position arrangements from the viewport width using `AnyLayout`, preserving the prior width thresholds, maximum content width, domain order, Money, colours and scales. The complete diagnostic build then reached canonical readiness in 4.4 seconds. The final product, without the temporary instrumentation, reached verified canonical startup in **5.448 seconds**, measured through the existing observational startup probe. These are startup observations, not warm tab-switch measurements. An exploratory bar-rendering substitution was not retained; `DashboardActivityComparisonView.swift` is unchanged. All temporary print/omission probes and the unsuccessful default-window-size experiment were removed. No shared exact-value helper, Transactions layout or financial calculation was changed by that layout correction, and no appearance/profile setting was edited. The later removal of Dashboard planning-FX presentation is recorded above.

After the correction, the owner still reports **approximately five seconds switching to Transactions and two seconds for other tabs**. This remains unresolved. The current diagnosis establishes a Dashboard layout cost; it does not establish the cause of every navigation delay. The owner requested keeping the corrected changes for testing and including both the exact fixes and a long-term proposal in the final report. The owner now requires a dedicated post-Sprint-94 measured performance correction and retaining this populated disposable Current Database as its realistic workload. No further navigation optimization is authorized in Sprint 94; no broader Transactions or shared-store performance implementation is included.

**Long-term proposal — not implemented or scheduled:** retain one canonical ledger as it grows. Coalesce notifications from one canonical publication; compute/cache Dashboard projections by provider generation plus store revision; cache Transactions rows, filter/sort results, menus and measured amount widths instead of rebuilding them from separate view getters. Invalidate after the relevant import, category/account/planner edit, restore or provider replacement. If measured history growth subsequently warrants it, add indexed repository reads and stable paging/virtualized presentation for visible transactions, retaining exact full-scope summaries and complete history. Heavy computation may use immutable snapshots off the UI actor only after its isolation and cancellation/publication contract is reviewed. Measure genuine accumulated databases and record navigation, initial hydration, projection and memory costs; no fabricated statements or derived financial evidence files. Roughly five statements per month means about 60 per year; it does not establish a fixed transaction count or certify future years of data. No new sprint number, background service, alternate workspace or data-retention cutoff is selected by this proposal.

## Source observations

The [official Al Dar portal](https://www.aldarexchange.com/aldarportal/Home) exposes the public forward request in its Home JavaScript. September 2026 discovery observed successful proportionate samples in the inspected band and bare JSON receive decimals. This does not establish a universal rate, absence of tiers, guaranteed settlement or safe reverse conversion. The response supplies no authoritative provider time, expiry, fee or executable quote ID; LedgerForge records its fetch instant and explicit amount/direction binding.

The earlier provider inquiry was an unsent proposal. Its personal-use gate was superseded by the owner's explicit access and architecture decisions. Historical observations are not additional Sprint-94 production-client executions.

## Boundaries and remaining work

No historical conversion, forecast, global/net-worth/investment FX, reverse endpoint, fee inference, remittance execution, broader Salary redesign or source ingestion. Salary-to-bank reconciliation remains separate and requires genuine evidence; payroll components never manufacture transactions. The owner-approved ordinary app regression plan passed on the final owner-corrected fingerprint; no unrelated parser/source-corpus campaign was run. Appearance/profile preferences remain owner state. PERSONAL-V1 remains NOT YET ADOPTED.

Sprint 94 is accepted and exact-path publication is owner-authorized. Broader FW-P3-08 and FW-P3-19 are not completed by this slice. Sprint-95 Budget Planner and the required post-94 corrections remain separate; closure implements none of them.
