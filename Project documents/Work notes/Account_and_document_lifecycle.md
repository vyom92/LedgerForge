# Account and document lifecycle

<a id="packet-account-lifecycle-packet"></a>
<a id="packet-document-library-packet"></a>

**Owners:** [FW-P2-01](../FUTURE_WORK.MD#fw-p2-01), [FW-P2-30](../FUTURE_WORK.MD#fw-p2-30), [FW-P2-31](../FUTURE_WORK.MD#fw-p2-31), [FW-P2-32](../FUTURE_WORK.MD#fw-p2-32), [FW-P2-33](../FUTURE_WORK.MD#fw-p2-33), [FW-P2-34](../FUTURE_WORK.MD#fw-p2-34), [FW-P2-35](../FUTURE_WORK.MD#fw-p2-35), [FW-P2-36](../FUTURE_WORK.MD#fw-p2-36), [FW-P2-37](../FUTURE_WORK.MD#fw-p2-37), [FW-P2-38](../FUTURE_WORK.MD#fw-p2-38), [FW-P2-54](../FUTURE_WORK.MD#fw-p2-54).

Substantive unresolved evidence, not accepted architecture or execution authority. Topic/owner order inherits [Guide rule I](../Project_Guide.md#documentation-order); dated observations follow the current conclusion, newest first. Original evidence interiors preserve their semantic order.

## Question

What account metadata/lifecycle or document access behavior should the owner be able to control?

## Current conclusion

Repository discovery found schema `AccountStatus` values active/archived/closed and historical `closed_at` capacity, but DTO/repository query/hydrator publication did not establish the proposed account lifecycle. Queries primarily expose name/ID behavior. Schema capacity is not an implemented workflow.

Targeted account display-name mutation is already accepted; the open metadata card concerns its broader remainder. A smallest additional metadata slice proposes one owner-selected field with validation, DTO/provider mapping and canonical reload while preserving immutable identity. Notes, custom colour/icon, institution logos, groups/favourites/order are separate owner choices, not implied by metadata editing. Logos need permitted assets, exact mapping and neutral fallback; they are not financial identity evidence.

Archive/restore, hide and close are distinct operations. Approve transitions, whether new imports may target each state, visibility/navigation and financial totals, closure date/source, post-closure imports and restoration. Hiding cannot silently change net-worth membership. Imported history and identifiers remain intact unless a separately approved financial mutation requires more.

The proposed document library can start with existing metadata and relationships: safe display labels, source type/profile, date/coverage only where known, account/session links and truthful full/duplicate/partial/legacy-unknown outcomes. It must not invent a complete file archive or treat a fingerprint as a live file URL. Current durable metadata has no retained-original or security-scoped bookmark authority.

Reopening is a separate unresolved choice: metadata-only browsing; owner reauthorization with fingerprint checking; or explicitly approved retained bytes and access/retention architecture. No selected option or stored originals are implied. Metadata-only browsing was proposed as the smallest boundary with no migration; that is a recommendation, not accepted architecture. Read-only navigation must preserve missing/legacy facts rather than borrow an unrelated session label for a document.

<a id="manual-gmail-feasibility"></a>
## Manual Gmail feasibility and storage audit — 2026-09-16

**Current completion:** discovery and matching review **COMPLETE**, including the explicitly approved older HDFC sender; historical storage audit **COMPLETE WITH STATED LIMITS**; direct app-owned OAuth/RAM retrieval and existing-importer isolated-memory campaign **RUN / PASS WITH STATED LIMITS**. Combined-bank source comparison is **COMPLETE WITH STATED COVERAGE GAPS AND UNRESOLVED SOURCE DIFFERENCES**: all nominated combined attachments were inspected; BIFF numeric rows remain independently unread and no combined layout is accepted production support. Documentation applicability remains separately tracked in the accompanying manifest. Account, dates, all-folder scope, RAM-only retention and sender decisions are settled; no owner decision is awaited. Documentation integration is tracked separately in the accompanying applicability manifest. No product email feature is implemented. Reviewed `main@cdfb7565893c90a138ddbbfa8beebdfbad98f8f8` with active Sprint 95 WIP. The [owner decision](../SCOPE_DECISIONS.md#manual-gmail-intake) selects RAM-only exact original attachments and manual email dates. Originals retention is settled for this path; no original attachment or actionable pending preparation survives restart. Reopening under FW-P2-01 remains unresolved. Connector attachment materialization is a **test-harness limitation**, not a product feasibility blocker.

**Historical metadata discovery, before the direct pilot.** All dates below describe email delivery in Asia/Qatar, not financial/statement periods or a promised schedule. The keyword search drained three pages (275 messages); checks using actual observed sender addresses added 11 attachment-free messages, giving 286 unique emails and 516 named MIME parts. All 286 returned timestamps were inside the selected half-open interval. Metadata/MIME triage: 73 ambiguous/unvalidated messages, 33 known-institution document categories outside current supported families, and 180 non-statement/out-of-scope messages. The 73 include 70 deliveries resembling accepted bank/card/salary families (including the ad hoc salary payment), one sent/support reply containing three PDFs, and two CAMS statement-related emails with no attachment. These are **not** validated supported sources, duplicates or password outcomes: no attachment bytes were retrieved or compared.

**Historical 14-address narrowing:** after individual review of the 13 additional senders, 14 addresses are included and five excluded. The included senders produced 132 messages: 44 roster/non-statement messages, 86 attached statement candidates, and two attachment-free CAMS capital-gain messages. A fresh paginated search using exactly the 14 approved senders, `has:attachment`, and the subject OR group `subject:statement subject:payslip subject:salary subject:"credit card" subject:"adhoc payment"` returned exactly **86 unique messages with no next page**: Axis bank 9, Axis card 8, CBQ bank 15, CBQ card 9, HDFC 9, Amex 8, salary 11, investment 17. All 86 were already inventoried and carry one PDF candidate each. This tested nomination query reduces broad discovery; it does not certify content support or exhaustive Spam/Trash coverage. The owner confirmed that Adhoc Payment, Salary and Payslip belong to the same source family; no download links were followed in the two CAMS emails.

| Family | Actual sender addresses; review status | Observed subject / filename and MIME nomination | Observed email cadence in selected range |
| --- | --- | --- | --- |
| Axis card | Owner confirmed `cc.statements@axis.bank.in` and `cc.statements@axisbank.com` | `Your Axis Bank ... Credit Card ...`; `Credit Card Statement.pdf`; `application/octet-stream` | 8 messages; January–July and September |
| Axis bank | Owner confirmed `statements@axis.bank.in` and `statements@axisbank.com` | `Access your Interactive One Glance Statement for ...`; similarly named PDF with whitespace before `.pdf`; `application/octet-stream` | 9; one email in each January–September month |
| CBQ bank/card | Owner confirmed `estatements@cbq.com.qa` and `estatements@cbq.qa` | Bank `Commercial Bank e-Statement for the month of ...`, `stmt-<reference>.pdf`; card `Commercial Bank Credit Card Statement dated ...`, `CardStatement-<reference>-<date>.pdf`; `application/pdf`; signed administrative statements from the excluded sender stay excluded | 15 selected bank and 9 card messages; multiple bank deliveries in some months; card one per January–September month |
| HDFC bank | Owner confirmed `hdfcbanksmartstatement@hdfcbank.bank.in`; continuation additionally approves `hdfcbanksmartstatement@hdfcbank.net` | `HDFC Bank Combined Email Statement for ...`; `<name>_<date>_<reference>.pdf`; `application/octet-stream` | 9; January–August, including two August deliveries |
| Amex | Owner confirmed `E-Statement@americanexpress.com.bh` (case-insensitive match) | `Your e-Statement from American Express`; `AMEX CARD STATEMENT-<reference>.pdf`; `application/pdf` | 8; one per January–August month |
| Qatar Airways salary | Owner confirmed `peoplexnotification@qatarairways.com.qa` | `PeopleX Notification - Payslip - ...`, `PeopleX Notification - Salary - ...` and `PeopleX Notification - Adhoc Payment - ...`; matching `Payslip_...pdf`, `Salary_...pdf` and `Adhoc Payment_...pdf`; `application/pdf`. One salary source family; exclude `Roster Report` | 11 salary-family emails across January–August; delivery months differ from statement months |
| CBQ investments | Owner confirmed `cb_wealth@cbq.com.qa`, `cb_trader@cbq.com.qa` and `cb_trader@cbq.qa` | `Portfolio Holding Statement for the Month of ...`, sometimes `[WARNING: MESSAGE ENCRYPTED]`; `Investment Holding ..._Statement.pdf`; `application/pdf` | 10 across January–September, including two January deliveries; investment content remains unsupported/unvalidated |
| Investment registrars | Owner confirmed `samfs@kfintech.com` and `donotreply@camsonline.com` | Consolidated Account Statement; identifier-bearing PDF names; `application/pdf` or `application/octet-stream`. Two CAMS capital-gain messages have no attachment | 7 selected attached statements; request-driven mailback deliveries, not a reliable monthly personal-statement schedule |

The owner explicitly **excluded** `cbadmin@cbq.com.qa`, `enq_sbimf@camsonline.com`, `ecas@cdslstatement.com`, `exg.statements@paytmmoney.com` and `alerts@paytmmoney.com`. Their signed bank statement, SBI/CDSL investment statements/disclosures and Paytm trade/tax attachments must not enter this selected matching path. The earlier 14 addresses and subsequently approved older HDFC address remain nomination rules, not authenticated financial identity or parser-support proof. The broad discovery also found `noreply@cbq.com.qa`, `noreply@cbq.qa`, `noreply@axis.bank.in` and `e-tdscertificate@hdfcbank.com`: their nine cash-deposit receipts, EMI/no-dues letters and TDS certificates are outside current supported statement families. One owner-sent Axis support reply with three PDFs remains ambiguous; sender-only filtering must not call forwarded/resent bytes duplicates. All identified ordinary statement candidates used the same owner mailbox in `To`; that field did not distinguish institutions.

Normalize sender address case and display-filename whitespace, but never rewrite original attachment bytes. Do not require `application/pdf`: accepted-looking Axis/HDFC filenames arrived as `application/octet-stream`. MIME nesting must be traversed; `.p7s` signatures and image logos are not statements. Preserve encrypted originals and use existing Keychain/unlock handling. No ZIP/HTML-link/EML product intake is implied. Attachment filename path segments are untrusted display metadata, never a path to write. Do not use subject month as the email-date filter, presume one statement per month/account, or infer duplicates from names/sizes. Actual content and independent validation remain mandatory.

**Identifier matching discipline.** Preserve the explicit identifier label, source location (subject/body/filename), exact observed masking, and account/card type as separate hints. The selected metadata has multiple masked CBQ bank accounts; a separately labelled CBQ card-account reference; two different Axis product/ending combinations (Select/Magnus); and stable-looking Amex, salary and CBQ-investment filename components. Do not merge accounts on institution, subject month, partial digits or a shared reference. HDFC filename suffixes and KFINTECH/CAMS request identifiers change across deliveries and are not verified account IDs. The metadata-only stage did not establish NRE/NRO identity. The subsequent original-attachment study distinguishes source-labelled account sections, full versus masked identifiers and additional products; exact private identifiers and financial comparisons remain in Chat only. Generic footer text and changing filename references remain non-authoritative. Exact private values were reported only in chat, not stored in this Git-bound note.

Production verifies the owner's salary grouping: [QatarAirwaysSalaryPDFParser](../../Parsers/QatarAirwaysSalaryPDFParser.swift) accepts Payslip and Salary as `regularSalary`, Adhoc Payment as `adhocPayment`, within one `qatar-airways.salary.pdf@1` profile. Preserve document kind and per-attachment fingerprints. A salary filename employee-code hint is not implemented parser-owned financial identity: current salary preparation emits no `FinancialIdentifier` and salary persistence has no employee/account identity field.

**Historical connector read boundary and limits.** Only connector `get_profile`, `search_email_ids` and `read_email` calls ran. No attachment-download, mail-write or send call ran. Labels were unchanged for all 286 inventoried messages; 12 samples also retained message/history IDs, timestamps, sizes and visible text/MIME content across repeated reads. Attachment locators changed and were excluded only from that representation comparison; original attachment-byte identity was not measured. Broad connector capabilities do not prove the future app token has `gmail.readonly`. Searches explicitly requested `in:anywhere`, and a Spam/Trash-specific query returned zero; the connector does not expose the API `includeSpamTrash` flag, so exhaustive Spam/Trash coverage remains unverified. Future direct API code must set `includeSpamTrash=true`, exhaust pagination, verify each `internalDate`, and keep older ranges selectable. Last-successful-check is informational only.

**Per-input storage result.** Production picker and Finder file-URL drop acquire one RAM snapshot. CSV decodes RAM data; PDF unlock uses PDFKit/CoreGraphics data; XLS uses `lf_xls_open_buffer`; XLSX uses `Archive(data:)`. No production statement-copy/decrypted/text/ZIP-member disk writer or general Mail/promised-file/EML path was found. Pending queue items hold URLs; the active preview holds bytes/text/parsed values. Confirm/cancel/skip/failure invalidate the snapshot; cancellation waits for any active synchronous read to finish. No persistent pending queue or secure-erasure/swap guarantee exists. Database writes retain canonical ledger/provenance/fingerprints, not full statement payloads. Sources: [snapshot](../../Import/Models/SourceContentSnapshot.swift), [engine](../../Services/ImportEngine.swift), [queue](../../Import/Coordinator/ImportCentreCoordinator.swift), [readers](../../Import/Readers), [persistence mapper](../../Services/ImportPersistenceMapper.swift).

The earlier storage-audit snapshot, not a fresh schema assertion, used read-only non-creating SQLite transactions and found Current V19 and the running isolated Sprint 95 V20 database each have 55 non-internal tables including `schema_migrations`; separate Release is V17 with 52. All three have zero `attachments` rows / zero blob bytes, and no populated `storage_path`, `extracted_text_snippet` or `fingerprint_data`. Current `normalized_json` keys are only profile/version and `row_original` keys only digest. DB/WAL bytes were unchanged around the measurements. This is observed local state, not new schema acceptance.

Bounded metadata scans found sandbox Application Support contains SQLite/recovery/manifest artifacts, not statement extensions; app Caches is empty (0 files/0 bytes). Five pre-existing JSON/TXT files in sandbox `Data/tmp` total 349,315 bytes: `axis-card-private-result.json`, `axis_require_debug.txt`, `axis_production_result.json`, `axis_reference_aggregate_debug.txt`, and `LedgerForge-CBQV16Authentic/legacy-v16-evidence.json`. Bounded in-memory inspection of only these five existing files now classifies them as follows; no payload rows/text were copied or used as financial inputs:

| Existing temporary file | Bytes / observed creation | Content classification and provenance limit |
| --- | --- | --- |
| `axis-card-private-result.json` | 1,681 / 2026-09-07 | Historical Axis authentic-acceptance v2 summary: test-phase outcomes, source-oracle bindings and persistence/non-vacuity counters. Related test-writer family exists in history, but exact v2 source revision and invocation are unproved. |
| `axis_require_debug.txt` | 8,498 / 2026-09-01 | Four `financialOutputMismatch` diagnostics with `AxisCreditCardAuthenticAcceptanceTests.require` stack frames. Test family is evidenced; exact logger/redirection/command is unknown. |
| `axis_production_result.json` | 337,676 / 2026-09-01 | Historical v1 authentic-acceptance report with blind-source results, narration text and per-row digest/multiplicity evidence. This is derived financial development evidence. Historical v1 contract/write mechanism matches, but the inspected narration-text field is absent from committed source, so the precise producing revision/invocation is unproved. |
| `axis_reference_aggregate_debug.txt` | 214 / 2026-09-01 | Two `tripletReferenceCounts` aggregate-array diagnostics. No exact source/history marker match; producer remains unknown. Shared timing alone is not attribution. |
| `LedgerForge-CBQV16Authentic/legacy-v16-evidence.json` | 1,246 / 2026-09-07 | Historical V16 database-export provenance: database/source hashes, profile/version and graph/reconciliation counters. Exact schema is consumed by the current separately gated migration test; the external JSON producer/invocation is unknown. |

Historical writer evidence is `47a0561ed553145758aacca94159722155b73825:LedgerForgeTests/AxisCreditCardAuthenticAcceptanceTests.swift` (v1 contract, `LEDGERFORGE_PRIVATE_RESULT_FILE`, sorted JSON/atomic write). `ab17d4b8dacd35f428b54fc7bb28ffb26df0829e` contains an analogous later v4 writer, not an exact v2 match. The result-file writer was removed in `cdfb7565893c90a138ddbbfa8beebdfbad98f8f8`. The CBQ consumer is [AxisBankV17AuthenticMigrationTests.swift](../../LedgerForgeTests/AxisBankV17AuthenticMigrationTests.swift), `HistoricalCBQExport` and `historicalAuthenticCBQV16GraphPreservesMissingMinimumDueThroughV17`. No exact producing command was established from available provenance. All five predate the September 11 processing rule and this Gmail task. Their hashes/size/timestamps were unchanged by inspection; none was deleted. No current ordinary-import writer path matches these artifacts; their retained presence is historical development residue, not proof that ordinary imports accumulate statement copies. A future cleanup decision remains separate. The old exact-reimport temp directory is empty. Ordinary source files, canonical DB content, deliberate backup/recovery packages, OS/Mail representations and development artifacts remain separate ownership classes. Passive observations do not establish repeated-import cleanup or crash behavior. A process-lifetime set of consumed preparation UUIDs grows in `ImportEngine`; it holds nonfinancial IDs, not statement bytes, and is a maintenance finding for review.

**Efficient reuse of historical proof.** `SourceSnapshotConfirmationTests.preparationRetainsSnapshotAndConfirmationDoesNotRereadDeletedURL` was introduced in `7e1345e3817d3c3e91c24f881b962a48279fd73b` (2026-07-28), switched to an authentic source in `ab17d4b8dacd35f428b54fc7bb28ffb26df0829e` (2026-09-09), and still copies that source to a disposable temp URL before deleting the copy. It proves confirmation consumes the retained snapshot once; it does not prove Gmail behavior. Do not rerun the copied-statement setup. Reuse its assertions with a one-shot RAM supplier made unavailable after acquisition; existing CBQ EML acceptance already injects decoded original PDF bytes into `SourceContentSnapshot(bytes:)` without an extracted file. Product queue/retry metadata is still URL-shaped and needs a truthful memory-source entry before integration. No reader rewrite is indicated. That historical discovery did not run a source-unavailability round trip; the continuation exercises the one-shot RAM supplier and isolated memory-provider confirmation described below, without copying or deleting a statement.

**Minimal implementation recommendation; unaccepted design.** Use an owner-authorized desktop OAuth client with only `gmail.readonly`, direct selected-attachment GET into bounded memory through an ephemeral data session (no download task/file destination), and the existing snapshot/unlock/prepare/validate/duplicate/ordinary-commit pipeline. Future Sprint-98 batch authorization replaces repeated clean-item confirmation only after the selected gates are implemented and accepted. Keep the Gmail fetch locator transient. For repeated manual scans, a small metadata-only candidate receipt keyed by stable account + message ID + MIME part ID is needed: current import history cannot remember a pre-import dismissal. Preserve Skip as current-batch deferral; explicit Dismiss/Revisit can supply durable suppression, and password/transport failures must remain retryable. Current queue retry eligibility excludes password-required/incorrect-password outcomes, so the future email retry presentation needs an explicit adapter to re-enter existing unlock/preparation. Existing source fingerprints decide confirmed duplicates after bytes are acquired. Receipt location/backup integration remains for implementation review, not a new accepted migration. Never infer source disappearance from a failed/partial scan or let later email absence mutate confirmed ledger rows.

**Executed continuation: direct OAuth and RAM-only existing-importer proof.** A dedicated owner-selected Google Cloud project, enabled Gmail API and Desktop client were configured through Chrome. The owner completed consent. PKCE S256, checked state and loopback callback were used; token exchange/profile verification established exactly `gmail.readonly` and the selected account. Pilot tokens are isolated in a namespaced Keychain item; no connector or Mail token was borrowed. The first token exchange failed because the standalone Python installation lacked its default CA file; the client was corrected to use the macOS CA bundle with certificate/hostname verification still enabled, then authorization succeeded. Only `users.getProfile`, `users.messages.list`, `users.messages.get` and `users.messages.attachments.get`, plus OAuth exchange/refresh, are allowed. No mailbox-write method ran. The ten focused nonfinancial transport tests passed, covering exact scope, epoch bounds, read-method/path restrictions, pagination, inline/separate bytes, sizes, redirects and budget release on failure.

The direct sample is an encrypted, already-supported CBQ monthly original. Repeated decoded bytes exactly matched a current authentic local original. A separate Debug module was compiled against the primary writer's fixed 337-input fingerprint `3032e86ff4bc407c5dc3e281705db649b57e15c0f345d836e537f38bc11a0a8f`, unchanged before/after compilation, using separate DerivedData and module cache. A scratch `@testable` executable linked the real importer; bootstrap and bounded production Keychain lookup passed. The original import/replay/cancel campaigns used the production credential store. Recompiling the diagnostic verifier triggered a new macOS executable-access prompt; that pending process was cancelled. The final provenance check instead supplied the same established credential, obtained by the already authorized exact-scope system Keychain reader, through an in-memory pipe to the ordinary password provider. No password file, argument or log, connector credential, or changed Keychain access rule was used. It explicitly injected an in-memory provider, fresh stores/hydrator and one-shot snapshot supplier, and made the global provider unavailable. It did not launch the product or select Current/Release.

Actual preparation, unlocking, validation and ordinary isolated commit ran. The supplier was unavailable after acquisition. Reconfirmation of the same prepared object made no second graph; freshly retrieved exact bytes produced the normal duplicate result; cancellation invalidated its snapshot and preserved the accepted graph. Forced hydration with upstream access disabled preserved the canonical graph. Independent in-memory geometry interpretation matched prepared posting/transaction dates, signed native Money, running balances, narration modulo whitespace, page ownership and ordered multiplicity. Canonical hydrated financial fields, including the secondary transaction date retained through CBQ preferred-source evidence, matched. **Limit:** physical page is transient in the existing shared lineage and absent after hydration; source-token ordinals differ from independent transaction-occurrence numbering but preserve ordered provenance. The first diagnostic checked only the general provenance field; it was corrected to inspect the accepted CBQ preferred-source date boundary before reporting retention. Durable isolated SQLite reopen, crash recovery, native app responsiveness, production email UI and combined/investment parser acceptance were **NOT RUN**.

The sample retained message identity, labels and MIME content across before/after reads after excluding the rotating attachment locator; separately repeated original bytes matched. This is bounded read evidence, not proof against independent owner/Mail changes. No attachment, decrypted PDF, extracted text, per-row oracle or financial comparison file was written. Buffers and snapshots released on completed/cancelled paths. This is an app-controlled no-copy result, not an OS swap/secure-erasure guarantee. A nonfinancial LLVM coverage profile emitted by the linked Debug module into its initial working directory was attributed, preserved in scratch and removed from the repository; subsequent profiles use explicit task-owned paths.

**Measured concurrency boundary.** The same original and empty isolated ledger were exercised with serial retrieval and two retrieval workers in deterministic primary/replay/cancel order. Each run used the same Gmail read counts and produced the same import/duplicate/cancel outcomes. The measured campaign was 2.523 s serial versus 2.341 s with two workers; queued decoded-byte peaks were 178,870 and 357,740; conservative reservation peaks were 1,204,292 and 2,408,584 bytes; both returned active/queued bytes to zero. Preparation and commits remained serial. This small repeated-source measurement establishes bounded transport overlap, not a statistically established speedup, optimal worker count, mixed-format scalability or native UI responsiveness. PDFKit, parser registries and ordinary engine work remain serial until explicit thread-safety proof; legacy XLS already serializes libxls with a lock.

**Combined-source review boundary.** The local original coverage sets both bank lookbacks to 2025-04-01; the email interval is `[2025-04-01T00:00:00+03:00, 2026-09-17T00:00:00+03:00)`, epoch `[1743454800,1789592400)`. Searches used `includeSpamTrash=true`, pagination and final `internalDate` filtering. Only HDFC/Axis lookback was expanded. A bounded HDFC subject-gap nomination found the older `.net` sender, explicitly approved by the owner before attachment inspection. The current approved list is fifteen addresses; the five exclusions remain unchanged. Candidate, downloaded-original, independently inspected section, duplicate and accepted-import counts are separate. Detailed private account/section comparisons remain in Chat, not this note or an oracle file. All eighteen HDFC and twelve Axis nominated PDFs were opened independently in RAM. The HDFC financial-field overlap preserves both account identities, both date roles, order, multiplicity and controls, while narration/reference differences and authentically omitted summaries need profile-specific treatment. The Axis study finds displayed-order, running-balance and printed-total disagreements and additional unqualified deposit/insurance sections; it does not establish full equivalence or validate import. A corrected exact-epoch two-sender/subject gap query leaves an absent intervening month and earlier coverage gap; it does not prove no other statement family exists. The seven selected local PDF and three CSV representations were independently interpreted; all seven paired BIFF XLS numeric row sets remain explicitly unread. Hashes of all seventeen selected local originals match their initial baseline. Private comparison maps, amounts, identifiers, rows and source hashes remain in Chat only. These are bounded discovery outcomes, not new source support.

**Smallest proposed combined-bank seam.** One exact parent snapshot/fingerprint should own ordered parser-produced account sections with source ranges, identity/masks, account type, currency, periods, controls and all source rows. Each section needs an explicit mapping and source-backed duplicate/equivalence disposition. Current `FinancialDocument`, account choice, mapper, confirmed plan and receipt are single-account; card instrument sections do not solve multiple bank-account ownership. Do not fabricate separate financial documents from split PDFs. Revalidate the entire parent and every section against current provider/generation state immediately before one atomic parent commit. Any invalid/unresolved sibling holds the entire original under the selected policy. All-new, all-represented, existing-plus-new, partial-overlap, multiple representations, added activity and invalid-sibling cases require explicit source-backed disposition. Different hashes do not prove new activity. Existing HDFC PDF/XLS equivalence and CBQ lineage do not authorize generic HDFC/Axis combined overlap. Axis relationship statements can contain deposit/insurance products as well as bank activity; these require explicit section treatment and cannot silently vanish.

Future persistence must account for parent-to-section ownership, every row exactly once, per-section account mapping and a truthful parent completion receipt; schema/backup compatibility needs separate architecture acceptance. Proposed durable metadata receipts may retain account/message/part keys, fingerprints, dismissal/revisit and retryable outcomes without payloads. Prefer ledger-owned metadata with explicit restore reconciliation; receipt “imported” status cannot outrank the active ledger's duplicate/ownership evidence. Tokens remain outside backups. No receipt store, schema or backup change was made by this pilot.

Primary references: [Gmail search/timezones](https://developers.google.com/workspace/gmail/api/guides/filtering), [list and Spam/Trash](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/list), [message/MIME identity](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages), [attachment GET](https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages.attachments/get), [scopes](https://developers.google.com/workspace/gmail/api/auth/scopes), [desktop OAuth](https://developers.google.com/identity/protocols/oauth2/native-app), [ephemeral sessions](https://developer.apple.com/documentation/foundation/urlsessionconfiguration/ephemeral), [item-provider file-copy behavior](https://developer.apple.com/documentation/foundation/nsitemprovider/loadfilerepresentation(fortypeidentifier:completionhandler:)), [Mail attachment behavior](https://support.apple.com/guide/mail/mlhlp1123/mac).

## Evidence and references

2026-09-10 repository discovery at the two pinned refs. Accepted ADR-008/024/033/037/038 and exact source-document binding remain controlling. No local financial original or database was opened for this refactor.

- [Core/DocumentStore.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Core/DocumentStore.swift#L16); SHA-256 `f4bb0ff9bb28d34154d38216130b32e730757b27054a881e17250aa7776c9aec` — DocumentStore
- [Database/DTOs.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Database/DTOs.swift#L351); SHA-256 `3d481ce8c1d69cbef77687587628a5d8764922fe2d8a4bcd88c7cb4b0ecfd6b3` — AccountDTO
- [Database/DTOs.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Database/DTOs.swift#L351) — DTO does not carry status.
- [Database/Migrations.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Database/Migrations.swift#L150); SHA-256 `ff0e02b72b66486b89255907ce12aa73aaf1919c677c608e9b12c79ed78de6b5` — accounts.closed_at
- [Database/Migrations.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Database/Migrations.swift#L150) — closed_at schema capacity.
- [Database/Repository.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Database/Repository.swift#L248); SHA-256 `0c4416fa22817a54dc776f3973cf0b0e92aee209e667decc12e62de182b4f92f` — ImportSessionRepository
- [Database/SQLiteRepositoryProvider.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Database/SQLiteRepositoryProvider.swift#L1920); SHA-256 `29166a1e985c9c4f5b43e6d13243a82fa5ff5e94382747b3fb406ae0dc98b454` — account and accounts queries
- [Database/SQLiteRepositoryProvider.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Database/SQLiteRepositoryProvider.swift#L1921) — closed_at omitted.
- [Database/SQLiteRepositoryProvider.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Database/SQLiteRepositoryProvider.swift#L1937) — Current order is name/id.
- [LedgerForgeTests/TransactionListViewModelTests.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/LedgerForgeTests/TransactionListViewModelTests.swift#L65); SHA-256 `30c938da8a204989e27b1b070a352b8bd784cf111ef01dd2b04f6dd3f8f3641f` — detail provenance presentation tests
- [Models/Account.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Models/Account.swift#L18); SHA-256 `63732666f7e7174f5d38c7c3db4b61082aa8a435fec5dd6e060d3d07265939fc` — AccountStatus and status field
- [Models/Account.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Models/Account.swift#L18) — Status values do not settle hide semantics.
- [Models/Account.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Models/Account.swift#L18) — In-memory model capacity.
- [Models/Account.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Models/Account.swift#L18) — active/archived/closed enum exists.
- [Services/RepositoryStoreHydrator.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Services/RepositoryStoreHydrator.swift#L348); SHA-256 `40cbd69a8a470748fcb637cb10ae5597855d5a03771e165d8f551162d2b94117` — canonical account hydration
- [Services/RepositoryStoreHydrator.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/Services/RepositoryStoreHydrator.swift#L1936) — status not hydrated.
- [ViewModels/TransactionListViewModel.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/ViewModels/TransactionListViewModel.swift#L120); SHA-256 `7e2dbde244295009d00df9cf6f6f5606f2e123374c912c346df4b07936b1cbd9` — detailPresentation(for:)

## Unresolved decision or blocker

Owner product choice and an approved metadata/lifecycle or file-access contract remain missing. Do not bundle all account features, file retention and repair into one mutation.

## What would invalidate this conclusion

Current DTO/provider/hydrator evidence proving an already implemented workflow, or a new explicit owner field/access decision, changes the scope. Unknown identity, totals impact, retention authority or stale file fingerprint falsifies a proposed safe operation.
