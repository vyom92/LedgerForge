# Transaction and R1 workflows

<a id="packet-sprint89-data-algebra-packet"></a>
<a id="packet-sprint89-data-algebra"></a>
<a id="packet-sprint89-native-acceptance-procedure"></a>
<a id="packet-import-preview-responsive-placement"></a>

**Owners:** [FW-P2-04](../FUTURE_WORK.MD#fw-p2-04), [FW-P2-05](../FUTURE_WORK.MD#fw-p2-05), [FW-P2-06](../FUTURE_WORK.MD#fw-p2-06), [FW-P2-07](../FUTURE_WORK.MD#fw-p2-07), [FW-P2-08](../FUTURE_WORK.MD#fw-p2-08), [FW-P2-09](../FUTURE_WORK.MD#fw-p2-09), [FW-P2-12](../FUTURE_WORK.MD#fw-p2-12), [FW-P2-40](../FUTURE_WORK.MD#fw-p2-40), [FW-P2-41](../FUTURE_WORK.MD#fw-p2-41), [FW-P2-43](../FUTURE_WORK.MD#fw-p2-43), [FW-P2-44](../FUTURE_WORK.MD#fw-p2-44), [FW-P2-46](../FUTURE_WORK.MD#fw-p2-46), [FW-P2-47](../FUTURE_WORK.MD#fw-p2-47), [FW-P2-48](../FUTURE_WORK.MD#fw-p2-48), [FW-P2-50](../FUTURE_WORK.MD#fw-p2-50), [FW-P2-52](../SCOPE_DECISIONS.md#fw-p2-52), [FW-P2-79](../FUTURE_WORK.MD#fw-p2-79).

Substantive unresolved evidence, not accepted architecture or execution authority. Topic/owner order inherits [Guide rule I](../Project_Guide.md#documentation-order); dated observations follow the current conclusion, newest first. Original evidence interiors preserve their semantic order.

## Current conclusion

**Current conclusion — owner acceptance, 2026-09-14:** Sprint 91A is **ACCEPTED** under `SPRINT_91A_DARK_APPEARANCE_AND_VISUAL_FOUNDATION_ACCEPTED`, including dark-only local customizable appearance, responsive Settings and the current Dashboard visual foundation. The [accepted outcome](../Archive/Accepted%20outcomes/Sprints_90-99.md#sprint-91a) owns the exact scope, final Debug/native evidence, publication inventory and accepted **FINAL_BYTE_SMALL_WINDOW_NATIVE_RECHECK_NOT_COMPLETED** limitation. Regression tests remain **SUSPENDED / 0 new executions**; no repeat resize campaign, preference reset or further redesign is part of closure. Original System/Light planning is superseded history. Sprint 92 is **NEXT / NOT STARTED**; its entry uses the exact published closure ref.

[Sprint 89](../Archive/Accepted%20outcomes/Sprints_80-89.md#sprint-89) is accepted at `038af8d9ebebe66b3f17925416821941cfa33d3d` under `SPRINT_89_TRANSACTIONS_REFERENCE_ACCEPTED`. It completed FW-P2-03 and FW-P2-53, with only the necessary Transactions portions of FW-P2-48/50. Its settled search/filter/sort/total/selection/inspector behavior and Chat-accepted focused, build, independent in-memory-oracle, no-write, native and bundle-containment evidence belong in the [accepted outcome](../Archive/Accepted%20outcomes/Sprints_80-89.md#sprint-89), not in this unresolved-work note.

Saved filters remain separate work: their preference scope and persistence contract are not implied by transient Sprint-89 filters. Sprint 89 does not select transfer matching, analytics, hidden FX, spending/income inference, parser/source-family work, persistence/schema changes or a formal accessibility programme.

[Sprint 90](../Archive/Accepted%20outcomes/Sprints_90-99.md#sprint-90) is accepted at `619c39ec07402a63c90b646cbba9ced806f5e99d` under `SPRINT_90_DASHBOARD_NATIVE_CURRENCY_HIERARCHY_ACCEPTED`, completing FW-P2-78. Its native-currency bank/card hierarchy, saved-plan reuse, read-only activity, responsive layouts and three ordinary keyboard actions are settled; detailed acceptance and the bounded calendar-day crash correction belong in that outcome record.

<a id="sprint-91a-visual-conformance"></a>
## Sprint 91A — accepted outcome and historical execution

The owner accepted the accumulated result on 2026-09-14 and authorized publication. [The accepted outcome](../Archive/Accepted%20outcomes/Sprints_90-99.md#sprint-91a) supersedes the pending/review-ready/stop statuses below without rewriting what was observed or unperformed at those checkpoints. The current saved appearance is user state; approved Reset defaults remain separate. The intentional UserJourney deletion is preserved.

<details>
<summary>Historical 91A execution checkpoints before owner acceptance</summary>

All candidate, pending, suspended-publication and stop statements inside this record describe their original checkpoint. They are not current acceptance or publication restrictions. The regression-test suspension continues under the final closure instruction.

**ACTIVE CANDIDATE / NATIVE VISUAL REVIEW PENDING / NOT YET CHAT-ACCEPTED.** The owner explicitly selected **SPRINT 91A — R1 Visual Foundation + Dashboard Visual Conformance**, starting at `ee547a46a126426275f5ecddadf13f769f6b1651`. It corrects shared theme/panel/shell rendering and Dashboard composition under [FW-P2-48](../FUTURE_WORK.MD#fw-p2-48). At initial authorization, original Simple Appearance Choice and Sprint-92 polish remained separate. The subsequent [dark-appearance continuation](#sprint-91a-dark-appearance) now supersedes original System/Light planning within 91A; Sprint 92 remains unimplemented and numbering is unchanged.

**Historical r22 local state — 2026-09-14, before the dark-appearance continuation:** visual build r22 compiles in Debug with no source warning; the existing App Intents metadata notice remains. The shared header and destination canvas use one continuous native indigo/slate gradient. Dashboard retains its 92% charcoal card wash and approved headline composition. The later owner-authorized consistent sidebar, Transactions system typeface, page-wide slate surfaces, stable alternating/selected row backgrounds, and column-header sorting are implemented locally. In the actual running r21 Transactions page at the restored window size, inline native inspection observed matching summary/chip/inspector surfaces, stronger slate selection, alternation retained across row selection and search focus, no violet table-container outline, and working header direction toggles. Sorting and scrolling retained the selected transaction's inspector; the table was returned to date-descending order and its top rows. No private captures were written to disk or Git. The full native appearance remains subject to owner review. Regression testing and window resizing remain suspended. Earlier test/build/capture results below belong to earlier source bytes; no final candidate acceptance, commit or push is claimed.

The owner subsequently found the accepted Sprint-90 Dashboard text-heavy, cluttered, visually flat and insufficiently faithful to the approved dark-glass references. Sprint 90's accepted data, interaction and day-change crash correction remain intact, including FW-P2-78's technical completion and its original acceptance token. Its tests/resize checks did not establish sufficient reference-versus-native visual fidelity. This is a dated qualification of the earlier outcome, not a claim that the original review discovered the gap.

The [shared handoff](../UI%20Assets/LF-UI-2026-09-R1/DESIGN_HANDOFF.md#authority) assigns authority by responsibility: owner/accepted financial rules control meaning; approved images control appearance/composition; responsive/state sheets control their conditions; numeric tokens control named roles. The existing [SC-05A](../UI%20Assets/LF-UI-2026-09-R1/LF-UI-2026-09-R1_SC-05A_Cross_Screen_Conformance_Matrix.md) **Keep** classification supports reuse of component structure and is not a native material PASS. Missing shared foundation belongs to 91A, while independently remaining P1–P6 items stay for their existing later review.

The initial candidate used the existing dark appearance, native material chrome/content and supplied typography/spacing/radius roles. The following original scope is historical where superseded by the linked dark-appearance continuation. Only Dashboard content is recomposed: native bank/card totals lead compact account/source rows; a saved funding calculation retains its four existing outputs, while the owner-directed no-saved-current-month-plan presentation uses one compact unavailable explanation and Open Salary; three Recent Activity rows and genuine Import Activity remain read-only; the three route actions and authoritative Attention condition remain unchanged. Financial/model/parser/persistence logic, V17, accepted Transactions behavior and the day-change correction are frozen. No preference storage, SC-04 controls, new ADR or personal adoption is delivered.

**Native checkpoint correction — 2026-09-13:** the owner did not approve the first native appearance: the Dashboard still read as large dark form panels. Publication remains prohibited. The task retains the owner's Sprint **91A** identifier; the feedback's “90A” label does not renumber it. The following bounded implementation map applies before the second source correction:

| Component | Reference / owner requirement | Concrete correction | Preserved behavior |
| --- | --- | --- | --- |
| Shared shell material | SC-01 native glass chrome; financial content remains readable | Remove the masking painted tint and use supported native glass for the existing navigation/toolbar surfaces | Existing destinations, import action, sizing, rail, focus and root ownership |
| Dashboard position | SC-03A hierarchy; owner rejects one enclosing bordered slate | Remove the outer position panel and internal dividers; give Bank/Card separate opaque depth, icons and fixed 28-point Money; use two domains across one-currency width when they fit | Exact currency/account membership, domain totals, Money, every applicable source date and uncertainty |
| Account/source rows | Supporting detail subordinate to the headline | Tight aligned account rows and one wrapping source-context line; no width-dependent type scaling | Full account amount and original date/context strings |
| Funding / supporting activity | One compact no-plan state; less competing panel weight | Branch on the existing empty state before the four metrics; lighter heading/action rhythm and fewer enclosing surfaces | Four real calculation outputs, distinct unavailable/partial states, three recent rows, genuine import outcome and the same three actions |

The revised source must receive fresh affected validation. The next owner submission must show the final tested 1440 × 900 and 1024 × 768 build first, with 640 × 768 only as responsive proof. The prior candidate's bounded live resize/scroll observation left database bytes, WAL bytes and SQLite data version unchanged; this does not substitute for the revised candidate's remaining route/six-screen/no-write checks.

**Coordinator precision for the same correction:** the compact currency identifier shares the subordinate position-heading line, and each bank/card icon, supporting domain label and fixed 28-point amount forms one group. Existing aligned account rows and their individual dates stay intact. The empty funding panel uses “Current-month Salary & Funding”, “No saved plan for the current month.” and a compact Open Salary action. This is local role/spacing/state composition, not another global font adjustment. Native appearance remains unapproved.

**Material layer decision:** native SwiftUI glass over the custom shell's nearly uniform opaque root produced an even pane in the native inspection. The small background-only `NSVisualEffectView` bridge therefore provides `.sidebar` / `.headerView` material with `.behindWindow` blending and no painted tint. Existing actions use native SwiftUI glass button styles. Financial position surfaces remain opaque; other shared content panels retain regular native material. This changes no window, lifecycle, responder or preference ownership and does not claim a native material PASS before pixel inspection.

**Owner color direction — 2026-09-14:** the owner explicitly approved the actual header's charcoal material color and identified the navy Dashboard canvas/card region as the mismatch. Preserve the untinted native chrome. The Dashboard alone uses a charcoal canvas and opaque charcoal financial surfaces to harmonize with that approved header; this is a bounded owner-directed appearance qualification, not approval of the complete native screen. Other destination backgrounds, original token/image bytes, typography, exact Money/source context and behavior remain unchanged in this color pass. The rejected proposed navy chrome tint is not part of the candidate.

**Alignment correction map — 2026-09-14:** owner feedback on the complete composition identifies uneven alignment/hierarchy. Align the top financial/funding modules and the supporting Recent/Import row on a common grid. The owner separately approved the existing leading-icon/domain-label/28-point-headline style; preserve it. Indent the account breakdown to the headline's text edge, use regular secondary-size names and full constituent amounts, retain subordinate dates and the existing trailing amount alignment. These are fixed semantic type roles, never window-dependent shrink-to-fit. Place each recent row's exact date with its supporting account/category/effect context beneath the description/full amount. Give the three existing actions the same compact native control treatment. At wider constrained layouts, the existing empty funding message and action may share a compact horizontal band; narrow presentation reflows. No selector, value, date, membership, action or non-Dashboard local layout changes are authorized by this map. Header-color and domain-headline approval are partial approvals only; the full native Dashboard remains unapproved.

**Owner direction — 2026-09-14:** all regression testing is suspended until further notice. The most recent 26-test run had already completed before this instruction; no further suite or regression campaign is authorized while suspension remains. Work continues on native appearance and builds needed to inspect the UI. Prior technical results remain tied to their tested source bytes and do not approve later visual edits. Publication and native acceptance remain withheld.

The owner's further activity-spacing feedback is addressed by a content-sized description/amount grid with a shared trailing numeric alignment and 24-point internal gap, plus a 32-point gap before the supporting column. Exact date/account/category/effect context remains beneath each row. A vertical presentation fallback preserves complete descriptions and Money when the natural grid cannot fit. This is a Dashboard rendering correction, not a new table workflow or a financial change.

**Owner material clarification — 2026-09-14:** Recent Activity and Import Activity require their own visible card containers. Restore those two containers without restoring the enclosing currency slab or spreading the activity amount column. The requested glass character is approximately 70% opaque / 30% translucent. Dashboard card backgrounds therefore combine a 0.70-opacity charcoal wash with a native behind-window blurred backdrop; foreground text, exact Money and controls remain fully opaque. The shared background-only bridge is reused by the unchanged header/sidebar material, with no new window/responder ownership. This owner clarification qualifies the earlier opaque-card preference; full native appearance remains pending.

**Owner continuity and typography feedback — 2026-09-14:** preserve the approved card appearance and extend the approved header material through the Dashboard canvas, removing the horizontal material break without changing scroll or window ownership. Transactions uses a separate, pre-existing monospaced typeface for Money. Match the shared system typeface with tabular digits at its existing table, summary and inspector sizes; update the pure text-width measurement to the matching font. Exact formatted values, table density, columns, selection, filters and inspector behavior remain unchanged.

**Explicit owner sidebar decision — 2026-09-14:** “Use consistent width and Collapse/Expand on all six.” This specifically extends 91A's original frozen sidebar boundary. All six destinations use the existing compact 208-point expanded sidebar, 64-point rail, and real Collapse/Expand control. The owner's explicit choice is shared as transient root UI state, with no stored preference. Dashboard's automatic 1000-point rail threshold remains; other destinations use the existing Transactions automatic 1280-point threshold unless explicitly overridden. Destination window minima, data/selection/reset ownership and financial behavior remain unchanged. The earlier decorative Collapse label is replaced only under this explicit authorization. Regression testing remains suspended.

**Latest owner native direction — 2026-09-14:** after the continuous backdrop appeared in the app, the owner found the 70% card treatment too transparent against it. Increase only the Dashboard card wash to 92% opacity for stronger separation; keep the native blurred layer, fully opaque text and approved headline composition. This later visual direction supersedes the earlier approximate 70/30 target. Window resizing is suspended until the appearance is correct at the owner's native viewing size; regression testing also remains suspended. No further dimension-specific acceptance or final native approval is claimed.

**Owner indigo/slate direction — 2026-09-14:** the owner approved the Import screenshot's highlight/tint/font character and asked to replace the grey top bar with a related color, optionally darker at the top and subtly lighter below, while preserving Dashboard translucency and card styling. Add one restrained indigo/slate gradient wash over the existing native header material. Reuse that same background for the continuous Dashboard canvas so its earlier horizontal material break does not return. Preserve the current 92% Dashboard card treatment, foreground typography, Indigo controls, sidebar selection, financial data and workflows. This explicitly supersedes the earlier untinted-grey header preference. Inspect only the actual running app at the restored native window size; resizing and regression tests remain suspended.

**Continuous-gradient correction — 2026-09-14:** the owner identified the remaining sharp header/content break on Transactions. The first tint pass still gave non-Dashboard headers a separate short gradient above each destination's painted canvas. Correct the layer ownership: one full-height native gradient behind the shared header and all six destination canvases, no separate header gradient or visible header divider, and no masking page-level fill. Preserve interior cards, tables, inspectors, popovers, controls, fonts, existing scroll owners and financial behavior. This is a shared background correction; other destinations' content is not recomposed. Native review is required at the current window size, with resizing and regression testing still suspended.

Native inspection of the continuous canvas exposed one direct contrast regression: Transactions' existing summary-tile fill became nearly indistinguishable from the new background. Those same tiles therefore use the existing charcoal financial-surface role. Their values, membership, text sizes, padding, radius, horizontal scrolling and display-only behavior remain unchanged.

**Owner selection/focus direction — 2026-09-14:** remove the violet outline around the entire Transactions container and any equivalent large-container treatment elsewhere; selected data rows should use a stronger shade of the same slate/indigo palette. Remove the Transactions table's focus-dependent violet overlay and suppress only its container focus effect, retaining focusability, native selection behavior, checkmark and ordinary field/button focus cues. Use a shared slate/indigo data-selection color rather than recoloring financial text. Inspect equivalent data-row/container styling without changing workflow or keyboard-navigation settings. Regression tests and resizing remain suspended.

The bounded source review found no equivalent focus-driven full-container outline on the other ordinary pages. Accounts' selected data row uses the same slate/indigo selection role, preserving its exact selection callback and membership. Sidebar focus/selection controls, Import's active drop-target and current workflow indicator, and static panel edges retain their distinct purposes.

**Owner Transactions palette and sorting direction — 2026-09-14:** the same slate/indigo family must cover all custom surfaces on the page, including alternating rows, selection, summary tiles, category chips, field backgrounds and inspector sections. Vary intensity and translucency within that family; retain opaque text, financial status meaning and ordinary control focus. Native inspection showed that SwiftUI's table tint does not replace the system blue selection or dark alternating rows. A narrow, background-only AppKit bridge may style the existing native row views through their public color properties; it must not replace the table, its delegate, selection bindings, responder chain or ordering. The owner also explicitly replaces the bottom sort menu with discoverable column-header sorting and subtle indicators. Reuse the existing native column comparators and direction toggles, preserving financial sort semantics and the matching-count footer. These are authorized local presentation changes; tests, resizing and publication remain suspended.

The row bridge's native inspection identified AppKit resetting `backgroundColor` on selection changes. The final rendering approach uses one noninteractive color layer beneath each existing row's cell content, detached with its background representable and sized by the existing row. It suppresses system-colored selection drawing only; native row selection, its checkmark, comparators, column headers and responder ownership remain intact. This avoids retaining a custom stripe treatment that disappears during ordinary selection.

**Owner sidebar color refinement — 2026-09-14:** the owner reports that Transactions now looks much better and asks for its charcoal sidebar to share the page's color scheme while remaining distinguishable. Add a slightly lighter, muted indigo gradient wash over the existing native sidebar material, preserving translucency and the existing dividing edge. Apply this through the shared sidebar used by all six destinations and its icon rail. Preserve approved Transactions surfaces, foreground text, navigation highlights, sizing, Collapse/Expand and all callbacks. This is a sidebar background correction, with native visual inspection at the existing window size; regression tests and resizing remain suspended. The owner's positive Transactions feedback is not acceptance of the complete Sprint-91A candidate.

Native r22 inspection in the active Transactions window confirmed the sidebar now reads as a lighter indigo surface beside the darker continuous canvas. Its gradual top-to-bottom shift, navigation highlight, text, footer and dividing edge remained visible. Debug compilation passed with no source warning; the existing App Intents metadata notice remains. The preceding r21 table/selection/header source is unchanged by this sidebar-only refinement. No resizing, regression suite, system-setting change, commit or push was performed.

Acceptance requires actual Current Database captures at 1440 × 900 and 1024 × 768 with a direct SC-03A/B comparison, followed by explicit owner approval of the native appearance before publication. Required constrained/native and technical checks remain distinct from that approval. On 2026-09-13, the owner authorized a **Sprint-91A-only complete-TestPlan exception** because the cross-cutting trigger applies but the current full plan requires a corpus campaign and derived financial files prohibited by this task. The 26 Dashboard/Transactions regressions, Debug/optimized Release, six-destination checks, bundle containment and in-memory no-write verification remain required. The exception does not change the Harness or establish recurring permission.

**Earlier candidate operational evidence — 2026-09-13, not the latest source bytes:** that candidate passed Debug and optimized Release under Swift 6, with no source warnings; only the existing App Intents metadata notice remained. Its focused set passed **26 definitions / 26 executions / 26 passed / 0 failed / 0 skipped** (9 Dashboard, 17 Transactions), including the corrected background-day notification regression and read-only in-memory Current Database checks. Both bundles excluded UI Assets. Twelve approved image files, numeric tokens and the original Sprint-90 outcome body were byte-preserved. These historical checks do not validate subsequent visual/sidebar edits or establish the still-pending final native comparison, six-destination checks or live navigation no-write check. No commit/push or native visual approval is claimed.

**Universal visual consolidation — 2026-09-14, Sprint 91A pending native review:** the owner requests one small shared visual system, calibrated from the current Transactions view. The owner explicitly reconfirms **Sprint 91A and working Collapse/Expand on all six destinations**; the continuation attachment's 90A label and Transactions-only control description are superseded. Regression tests and resizing remain suspended; builds and normal-size native visual inspection are permitted. No publication is authorized.

| Affected component | Current owner requirement | Concrete correction | Preserved behavior |
| --- | --- | --- | --- |
| LFTheme and root presentation | One measured palette, named type/spacing/material/state roles | Supply one dark theme value through the SwiftUI environment; shared components and pages consume roles | No appearance selection, preference storage or lifecycle change |
| LFPanel and shared controls | Equivalent cards and actions use equivalent rendering | Centralize native material plus calibrated surface wash, generic panel variants and control roles | Existing card contents, actions, semantic status colors and approved headline hierarchy |
| Repeated label/value groups | Complete values share trailing alignment without drifting across the window | Reuse an intrinsic two-column grid with bounded gutter and a vertical content-fit fallback | Exact Money, constituent membership, source dates and context; no shrink-to-fit |
| Shell and destination styling | One continuous canvas and coherent selection vocabulary | Migrate duplicate fills/type roles; remove Developer Console's destination-wide background mask; reuse selected-data-row roles | All-six sidebar geometry/actions, native Table, selection identities, comparators and workflows |

The supplied rendered calibration is canvas **#0F1324**, dark content **#191F32**, control/row **#1D2235**, raised/card **#252D41**, neutral elevated control **#3F4251**, navigation selection **#3A3274**, foreground **#F4F6FE**. Native screenshot inspection places the 63/66/81 reading on **Hide details**, not the selected data row; the existing indigo data-selection treatment therefore remains the shared active/inactive selection role. Unmeasured accent, secondary text and semantic status values retain their existing definitions. The latest modular continuation reasserts approximately 70% surface weight / 30% material contribution for applicable cards; sampled rendered RGB values are not blindly given a second opacity. Financial foregrounds remain opaque. Only this work note is updated for the consolidation; no reference PNG or historical accepted-outcome body is rewritten.

**Consolidated candidate, native build r28 — 2026-09-14:** `LFTheme` now owns `Palette`, `Typography`, `Spacing`, `Radius`, `Materials` and `InteractionStates`. One `lfTheme` environment value is supplied inside the existing `ContentView` root, preserving the app's window identity and lifecycle entry point. `LFPanel` owns standard/inspector/subtle surfaces; shared secondary-action and input modifiers retain native controls and local focus cues. `LFLabelValueGroup` supplies an intrinsic, trailing-aligned value column and bounded gutter for Dashboard account, activity and available-funding groups, with a content-fit vertical fallback. No runtime token loading, theme registry or appearance preference was added.

The shell, common action/empty/filter/icon/info/status helpers, Dashboard and Accounts presentation, Transactions and its small existing native-row appearance bridge, Import queue/footer, Salary editor, category controls and Developer Console presentation consume the shared roles. Duplicate page/panel fills, selection colors, secondary text, repeated typography and ordinary input/action surfaces were consolidated. Layout-specific widths remain local. Native table/form density, source-editor and diagnostic monospace, and established financial/status colors remain deliberate exceptions; functional tables and Salary result composition were not replaced. An unused legacy metric helper retains its existing typography and is outside the rendered paths. A read-only presentation review identified paired Transactions inputs receiving a group-level focus modifier; r28 applies that modifier to each field individually.

Actual, unedited r28 app captures were inspected in memory at the owner's existing normal wide window size:

| Destination | Observed native presentation |
| --- | --- |
| Dashboard | Independent Bank/Card headline groups, compact aligned account/source context, one missing-plan message and Open Salary, restored Recent/Import cards, and all three established actions. |
| Transactions | Related normal/alternate/selected row colors, stronger table-focused selection, quieter selection with search focused, local search focus ring, header sorting cues and populated inspector; no page-wide violet focus outline. |
| Accounts | Selected account row and populated inspector share the palette; established financial effect colors remain visible. |
| Import | Ordinary idle page, genuine existing durable outcome/history and shared panels/actions; no active queue was present. |
| Salary | Existing editor, shared input/card surfaces, local field focus and consistent accent on existing controls; no input was edited or saved. |
| Settings | Existing system/category content, shared input/action surfaces and Developer Mode OFF. |

Developer Console was source/build reviewed but **NOT OBSERVED** natively because Developer Mode is off; it was not enabled. Hover was not pixel-verified because coordinate-based native interaction reports `noWindowsAvailable`; accessibility actions and ordinary keyboard focus were sufficient for the observed states. The Transactions filter popover appeared in the native accessibility tree, but the capture tool omitted its pixels; its individual-field rendering remains **NOT VISUALLY VERIFIED**. Multiple currencies, saved/partial funding plans and active import-queue states were not manufactured and remain **NOT OBSERVED** in this pass. Native material pixels vary with backdrop/focus; the calibration records the intended family, not exact equality for every rendered pixel.

**Historical r28 verification, before appearance preferences:** final Debug r28 **BUILD SUCCEEDED**, with **0 source warnings** and only the existing App Intents metadata-extraction notice. Its bundle contains no UI Assets/design-reference payloads. `git diff --check` passes. Models, view models, parsers, database, migrations, test sources, project membership, `LedgerForgeApp.swift`, approved images and numeric tokens are unchanged against the baseline. Regression tests remain explicitly suspended: **0 executions; no new pass/fail/skip result**. No final-byte Release build, resize campaign or instrumented navigation no-write check is claimed. Inspection used navigation, selection and focus only; no financial form submission, import or save was performed, and no private capture/evidence file was written. The index is empty and no untracked files were introduced.

At that r28 checkpoint, the candidate remained on baseline `ee547a46a126426275f5ecddadf13f769f6b1651`, uncommitted and unpushed. Source was frozen for **SPRINT_91A_MODULAR_THEME_NATIVE_REVIEW_READY**; owner visual review and the subsequently authorized validation/publication path remain pending. V17 and the day-change crash correction are preserved. No new ADR or preference system; original Simple Appearance Choice and Sprint-92 work remain unimplemented, FW-P2-49 remains **NOT REQUIRED-DO NOT CONSIDER**, and Personal-v1 remains **NOT YET ADOPTED**.

## Remaining R1 and interaction boundary

<a id="sprint-91a-dark-appearance"></a>
### Historical continuation — dark appearance editor, 2026-09-14

The owner explicitly authorizes **Dark-only, adjustable shared colours/tints, font family and hierarchy sizes, background/card opacity, responsive Settings** within the existing Sprint 91A candidate. This supersedes earlier exclusions on these controls and older SC-04 System/Light choices for this implementation; historical decisions remain preserved. Regression testing and publication remain suspended. LedgerForge-only resizing is authorized for native verification, with the starting size restored afterward.

Implementation map: one app-owned local appearance preference store resolves the existing `LFTheme`; the existing `ContentView` environment injection preserves state/window identity. Native color wells and font/size/opacity controls live in the existing Settings destination, whose cards reflow in a stable order and one scroll region. Shared font descriptors serve both rendering and native measurement; shared surfaces, secondary actions, table corners, sidebar label wrapping and Dashboard label/value width receive only the requested finishing. Financial inputs, calculations, draft/Save ownership, database and migration semantics remain unchanged. Restore defaults removes only appearance overrides. This continuation is pending native review, not accepted Appearance/Sprint-92 closure.

**Owner finishing and first inspection:** native r29 introduced the live editor and seamless native title bar. The owner approved that title-bar direction, then requested sidebar tint through its top edge, typography sliders and a clearer Settings hierarchy. r30 supplies those corrections: aligned colour rows, role-grouped sliders, and a responsive Application & data group before full-width Category Management. SC-04’s thin related-colour card edge is reused without changing approved palette defaults. The only `LedgerForgeApp.swift` change is `.windowStyle(.hiddenTitleBar)`; native traffic lights and scene ownership remain.

**Final owner refinements, 2026-09-14:** remove the repeated top-right Import Statement action from the shared page header; preserve the Import destination’s existing file chooser, empty-state actions and Dashboard Open Import route. Show untouched default-zero Salary inputs blank at the presentation boundary, retaining calculated zero results, explicit entered values and existing draft validation/Save semantics. These are the owner’s additional bounded corrections, not new financial behavior. Investigate the owner-reported Xcode `LFFontRole` diagnostics against the open project and final build before closing the checkpoint; a successful shell build alone does not dismiss the editor issue list.

**Dashboard correction after native feedback:** the r30 capture showed account/source and Recent Activity rows expanding vertically and overlapping adjacent cards. r32 groups each label with its own context in one intrinsic-height grid row, retaining a bounded trailing amount column and a no-shrink stacked fallback. The native r32 capture shows compact source context directly below its corresponding name/description. Exact amounts, currency, source dates and accepted three-row membership remain intact. This correction is presentation only; the complete Dashboard still requires owner visual approval.

**Native verification, r32–r34:** Settings was inspected at exact outer-window sizes 1440 × 900, 1200 × 850 and 1024 × 768. Its smallest supported outer window is **760 × 640**, including native title-bar/safe-area space; cards stack in the same order, with one scroll region and Restore reachable. The final r34 build was captured again at 1440 × 900, 1024 × 768 and 760 × 640. Starting bounds **position (0,39), size 1710 × 1073** were restored and read back after the final size checks. The prior intermediate-width observation concerns the unchanged Settings reflow implementation.

Native Avenir Next selection and body/table sliders at 18 points updated rendering immediately; Transactions showed complete values and taller rows with an explicit selected-row inspector. r33 additionally displayed the genuine Dashboard headlines at 48 points without reducing or truncating them; the approved 28-point role was restored. Dashboard measures domain/supporting widths using the same native font descriptor as its Money text, and supplies horizontal scrolling only when a complete value cannot fit. A populated funding plan and the actual extreme-value scroll branch were not manufactured or certified.

The owner manually selected a temporary green using the native colour panel after the automation tool could not expose that panel. The accent propagated to Dashboard, Accounts and Transactions; distinct navigation/data-selection roles and semantic status colours retained their meanings. Native Restore returned the approved palette, System family, role sizes and 86% background / 70% card defaults, and removed only the appearance preference key. Developer Mode remained off and existing category/data state remained visible. The owner’s prior **30% background / 50% card** choices were then restored and survived safe relaunches through r34. These owner choices differ from the approved reset defaults. The app remains dark-only.

**Final r34 native finishing:** all six ordinary destinations were inspected at their supported sizes. Settings has grouped size sliders, aligned native wells, responsive Application & data and full-width Category Management. Dashboard retains separate Bank/Card and Recent/Import cards, exact source context, compact missing-plan treatment and its three route actions; its full appearance remains unapproved. Transactions retains related row/summary/inspector surfaces, header sorting, complete Money and coordinated table/scroller corners. Accounts’ type badges and Salary’s Add commitment controls retain shared readable roles and genuine enabled-state logic. Import remains idle with its local file chooser and existing history; no source was selected or imported. The repeated top-right Import Statement action is absent from the shared header. Sidebar tint reaches the top border behind the native window controls; all-six Collapse/Expand implementation remains in place. The expanded Developer Console label was observed earlier with the owner-enabled gate, but the console itself is NOT OBSERVED in this final build because Developer Mode is off.

Fresh Salary zero-default inputs now appear blank, while calculated results remain numeric and the nonzero transfer-fee default remains visible. This uses view-local touched-field presentation and one **11-line read-only presentation query** in `SalaryWorkspaceViewModel.swift`; its existing raw draft, parsing, calculation, update and Save methods are unchanged. Saved/copied zeros are not blanked by that query. Clearing an edited field still follows existing validation. No plan was saved or manufactured for this observation; explicit-entry/saved-plan branches are source-reviewed only.

**Current source/build boundary:** the open Xcode project’s final Debug **r34 BUILD SUCCEEDED**, with **0 source warnings** and only the existing App Intents metadata-extraction notice. Executable SHA-256: `27ff5cd86e5f8e7523408919f27b39a4876e525abd596534854e6b7cf668e749`. The executable is in Xcode’s normal DerivedData product for the verified primary project. The owner-reported 26 editor errors cleared after the build and normal source-editor refresh: Issue Navigator **0 issues**, `LFTheme.swift` **0 diagnostics**, `TransactionListView.swift` **0 diagnostics**. No duplicate type or unrelated project workaround was added.

All 24 R1 manifest payload counts/hashes match; all 12 approved PNG payloads are byte-preserved, including the extensionless state sheet. The final app bundle excludes UI Assets/design-reference payloads. `git diff --check` passes. New source membership is exactly `LFAppearancePreferences.swift` and `AppearanceSettingsView.swift`. Financial models, calculators, parser, database/migrations, provider/hydration and day-change correction are unchanged. Newly authorized persistence is UI preferences only; V17 is unchanged and no ADR is added.

Regression tests remain **SUSPENDED: 0 executions in this continuation; no new test pass/fail/skip result**. No final-byte optimized Release or instrumented financial no-write comparison is claimed. Native inspection used appearance controls, read-only navigation/selection, safe relaunch and authorized LF-only resizing; no financial Save, category mutation or import confirmation was performed. Multiple currencies, populated/partial saved funding, malformed preference payloads, all installed families and font extremes, and active import-queue states remain unobserved. Private native captures stayed in memory/Chat and were not written to disk or Git.

The candidate remains on `main` at published baseline `ee547a46a126426275f5ecddadf13f769f6b1651`, with an empty index and no commit/push. This is **SPRINT_91A_DARK_APPEARANCE_SETTINGS_NATIVE_REVIEW_READY**, pending owner visual review; the token is neither sprint acceptance nor permission to resume tests/publication. Original System/Light planning is superseded history, Sprint 92 remains unimplemented, FW-P2-49 remains **NOT REQUIRED-DO NOT CONSIDER**, and Personal-v1 remains **NOT YET ADOPTED**.

**Targeted reference-informed finishing, 2026-09-14:** native Settings, Dashboard and selected Transactions were inspected before this batch. Card boundaries, enabled action labels and indigo selection already distinguish their roles; their shared rendering stays unchanged. `AppearanceSettingsView` now groups background opacity directly with Window & navigation wells and card opacity with Cards & raised surfaces, keeping all 11 wells, bindings and their relative order. The existing typography helper adds a nonblocking below-10-point readability cue. Size ranges, defaults, font roles and rendering/measurement descriptors are unchanged. Selected HIG macOS/layout/settings/typography/material guidance and the pinned Impeccable [critique](https://github.com/pbakaus/impeccable/blob/cb56ed6c19a07329a9fa0cd4e657bee040156593/skill/reference/critique.md), [layout](https://github.com/pbakaus/impeccable/blob/cb56ed6c19a07329a9fa0cd4e657bee040156593/skill/reference/layout.md) and [typeset](https://github.com/pbakaus/impeccable/blob/cb56ed6c19a07329a9fa0cd4e657bee040156593/skill/reference/typeset.md) prose informed these two corrections. No executable, detector, scoring, saved report or global skill change was used.

**Owner identity and persistence requests:** the supplied archived `AppIcon_v1.0.png` now populates the existing ten macOS app-icon slots. The sidebar reads the same native application icon instead of drawing a separate mark. A fresh read-only `gpt-5.6-terra` / high reviewer confirmed this narrow wiring; no project membership or Info.plist source changes were needed. The archived original remains byte-identical (SHA-256 `5593e9ce6ebeed8d564a6a671a4c4bdcf7cd79bcecf9903561fffccf74783aac`). During inspection, the owner selected newer blue accent/background/navigation/card/raised colours and explicitly instructed that they must persist without reset. Those newer choices supersede the initial review palette for the current session; approved Reset defaults remain unchanged. The existing appearance store already saves each change immediately. A normal quit was confirmed by process exit, followed by relaunch of the same final product with a new process. At the immediate post-relaunch inspection, the complete saved appearance dictionary matched the pre-quit snapshot, and native Settings showed the same five custom colours, System family, 28 pt headline override and 30%/50% tint opacities. The owner subsequently adjusted Accent and Selected navigation again; those later live changes were left untouched, not reset to the earlier verification snapshot. No new persistence mechanism or preference reset was necessary.

**Current build and native evidence:** final Debug **BUILD SUCCEEDED**, with **0 source warnings** and only the existing App Intents metadata notice. Running executable SHA-256 `734c2423fad9e7253fd0aeb86718930edaa78868baed265c10ea84eca284420a`; compiled Settings source SHA-256 `8195dad5d3f3f32be0c89d78373e7921c342d707e8e51f92aaf918c611390f8d`; compiled shell SHA-256 `5aedc00c96154370eac9e3dcc2e1eec6783b0422a9d4b008960d37f07d1b46f0`. Final source hashes still match. The final product's signature verifies, its ten app-icon renditions have the required dimensions, and UI Assets/design-reference payloads remain excluded from the bundle. Native Settings and Dashboard were captured at verified **1440 × 900** using genuine current data and the owner's newer colours. Settings' lower controls/readability cue were inspected by scrolling. Final Transactions captures show the same selected record with table focus versus Search focus; the field has its local outline, selection remains, and Clear filters stays genuinely disabled when no filter is active. No shared control-colour correction was needed. Private captures stayed in memory/Chat only.

**Remaining verification limitation:** AppleScript initially verified 1440 × 900, then repeatedly returned zero LedgerForge windows / invalid index `-1719`, including after foreground/activation attempts. Further retries were stopped. **1140 × 800 intermediate, 1024 × 768 and 760 × 640 are NOT VERIFIED on these final bytes.** Native Zoom returned the window to full size; the saved native frame is 1710 × 1073 after quit/relaunch, matching the starting size, but final AppleScript position readback was unavailable. Hover and direct Dock pixels were not captured (Dock tool timed out); bundled icon wiring and native sidebar pixels were verified. Developer Mode remains off. No populated/partial funding, additional currencies or active imports were manufactured. Regression tests remain **SUSPENDED: 0 new executions**; no final Release or instrumented financial no-write campaign ran. No further financial/model/parser/database change, migration change or ADR was introduced in this pass; the existing Salary presentation query and day-change correction remain intact.

The owner confirmed their separate deletion of `Project documents/UI Assets/Archived/UserJourney_v1.0.png`; it is preserved as owner work. The complete worktree has **52 changed paths**: the preceding 40, 11 app-icon metadata/rendition paths, and that owner deletion. The index remains empty at `main` / `ee547a46a126426275f5ecddadf13f769f6b1651`; no commit/push or acceptance closure occurred. Current checkpoint is **SPRINT_91A_STOPPED_FOR_CHAT** because constrained final-build size verification remains incomplete, not because appearance persistence failed. Overall and Dashboard visual acceptance remain with the owner/coordinator.

<details>
<summary>Exact candidate paths before this targeted pass (40, including prior uncommitted 91A work)</summary>

- `AppShellPresentation.swift`
- `ContentView.swift`
- `Core/LFConsoleButton.swift`
- `ImportCentreFooterRenderer.swift`
- `LedgerForge.xcodeproj/project.pbxproj`
- `LedgerForgeApp.swift`
- `Project documents/Archive/Accepted outcomes/Sprints_90-99.md`
- `Project documents/FUTURE_WORK.MD`
- `Project documents/PROJECT_STATE.md`
- `Project documents/SCOPE_DECISIONS.md`
- `Project documents/Sprint roadmap/LedgerForge_Roadmap_Sprints_90-99_Current.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/ACCEPTANCE.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/ASSET_MANIFEST.json`
- `Project documents/UI Assets/LF-UI-2026-09-R1/DESIGN_HANDOFF.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/DESIGN_TOKENS.json`
- `Project documents/UI Assets/LF-UI-2026-09-R1/Inherited_Screens.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/LF-UI-2026-09-R1_SC-05A_Cross_Screen_Conformance_Matrix.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/README.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/SC-01_App_Shell.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/SC-03_Dashboard.md`
- `Project documents/UI Assets/LF-UI-2026-09-R1/SOURCES.md`
- `Project documents/Work notes/Transaction_and_R1_workflows.md`
- `ViewModels/SalaryWorkspaceViewModel.swift`
- `Views/AppearanceSettingsView.swift`
- `Views/CategoryManagementView.swift`
- `Views/Common/LFActionRow.swift`
- `Views/Common/LFAppearancePreferences.swift`
- `Views/Common/LFEmptyState.swift`
- `Views/Common/LFFilterChip.swift`
- `Views/Common/LFIconTile.swift`
- `Views/Common/LFInfoRow.swift`
- `Views/Common/LFInlineBadge.swift`
- `Views/Common/LFPanel.swift`
- `Views/Common/LFStatusBadge.swift`
- `Views/Common/LFTheme.swift`
- `Views/DeveloperConsoleView.swift`
- `Views/DeveloperDatabaseProfileWarningView.swift`
- `Views/ImportCentreBatchViews.swift`
- `Views/SalaryView.swift`
- `Views/TransactionListView.swift`

</details>


</details>

<a id="sprint-92-handoff"></a>
## Sprint 92 forward handoff — next, not started

Use the exact published Sprint-91A ending ref and post-closure current state/roadmap/SC-05A. Reuse accepted 91A source and its limitations; do not restart its colour exploration, preferences, modular theme or waived final-byte resize work. The owner accepts the current Dashboard visual foundation. Preserve saved local appearance overrides and factory-default definitions separately; do not reset either for closure or inherit expired system-control permissions.

Original Sprint-92 owners remain **FW-P2-40, FW-P2-41, FW-P2-47, FW-P2-48 and FW-P2-50**. Refresh the historical Sprint-88 SC-05A against accepted 89–91A before resolving P1–P6; closure does not mark any finding fixed or convert N1–N3 observations into defects. Useful information presentation is an additional owner direction, with Dashboard the primary content/composition target and actual residual relationships across existing screens.

The complete v1 visual family informs density, hierarchy, compact tables, module proportions, spacing, alignment, use of width and contextual actions. Its old palette, sample balances, fictional analytics, profile blocks and unsupported actions are not authority. Current accepted financial/source/workflow meanings and 91A appearance remain authoritative. Review actual available pixels at Sprint-92 execution; this closure performs no new legacy-image visual audit. `UserJourney_v1.0.png` was intentionally deleted and must not be restored; state that reference gap. `DesignBoard_v2.0.png` is the archived companion/master board; AppIcon is branding context, and no Salary_v1.0 asset is assumed.

**FW-P2-46 remains the chart owner.** The separately supplied Sprint-92 implementation brief selects only an all-recorded-activity comparison of existing bank inflow/outflow and card increase/decrease owed, separated by native currency and domain. That prepared specification is retained for Sprint 92; no chart implementation or financial qualification occurs in this closure. Exact record membership, grouping, totals, withheld/missing effects and currency separation require an explicitly authorized independent in-memory genuine-data comparison before chart publication. Regression tests remain suspended. The interactive-chart backlog is not complete, and broad analytics, inferred spending/income/expenses, hidden FX, invented net worth and generated financial data remain outside the selected boundary.

The 93–100 sequence remains unchanged. No Sprint-92 implementation starts automatically after this closure push.


FW-P2-48 and FW-P2-50 retain cross-screen work beyond accepted 89–91A. Refresh [SC-05A](../UI%20Assets/LF-UI-2026-09-R1/LF-UI-2026-09-R1_SC-05A_Cross_Screen_Conformance_Matrix.md) before resolving remaining Sprint-92 findings; neither broad row is completed. [FW-P2-52](../SCOPE_DECISIONS.md#fw-p2-52) is complete in accepted 91A. Import Preview alternatives below remain proposals, with no A/B selection made by this closure. FW-P2-49 remains **NOT REQUIRED-DO NOT CONSIDER** and Personal-v1 remains **NOT YET ADOPTED**.

### DRAFT — Import Preview Responsive Placement (user feedback, 2026-09-10)

This subsection records subjective user feedback and an unaccepted layout proposal. It does not change the approved visual direction, accepted Import workflow, source interpretation or native acceptance status. The user reports heavy scrolling in the left prepared-import region while the right region beneath Validation Review has unused space. The supplied private screenshot supports broad layout geometry only; its financial/source content is neither transcribed nor published.

**Owner:** [FW-P2-48](../FUTURE_WORK.MD#fw-p2-48), including its bounded owner-usability checks. This is an unaccepted discovery proposal, not an implementation or native-acceptance claim. Coordinate with [FW-P2-67](../Archive/Accepted%20outcomes/Sprints_80-89.md#sprint-88) only when a selected shell decomposition affects this composition; no sprint is selected here.

**Candidate A, preferred for a sufficiently wide window:** place the existing read-only Transaction Preview beneath Validation Review in one right-column review stack. Keep the queue, prepared statement summary, account/identity and duplicate/equivalence explanation readable at left, with confirmation/cancel/skip controls visibly available. This placement is a suggestion, not a forced rule.

**Candidate B, responsive fallback:** place one full-width/shared Transaction Preview below the summary/review split when the right-side table cannot remain readable or validation content is long. Determine the breakpoint from content fit and text size. Do not duplicate the preview or create nested unbounded scroll owners.

Both candidates preserve source order/multiplicity, prepared status, identity/account review, validation, duplicate/equivalence semantics and explicit confirmation. They introduce no editing, filtering, sorting, source reopening, new selection semantics or financial/color change. Future native acceptance must cover broad/narrow windows, long validation, useful keyboard actions, scroll/focus and existing selection, footer visibility, cancellation/confirmation, and truthful empty states. A zero-transaction statement case requires genuine authentic evidence; if absent it stays untested. No native verification is claimed by this appendix.

## Evidence and references

The complete accepted Sprint-89 evidence is preserved in the [accepted outcome](../Archive/Accepted%20outcomes/Sprints_80-89.md#sprint-89). The following pinned references remain historical support for unresolved Transaction/R1 work; they do not revive the settled Sprint-89 proposal or claim a current runtime pass.

- [ContentView.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/ContentView.swift#L1507); SHA-256 `c315381adf31633a27671b14499fc5dd12d7b794af047c82cfb2acf67990202b` — contextualToolbar
- [ContentView.swift at af2d194](https://github.com/vyom92/LedgerForge/blob/af2d1949c6e9a73af3bb004422b72882d28195e2/ContentView.swift#L1443) — historical monolithic shell
- [Database/Repository.swift at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Database/Repository.swift#L208); SHA-256 `0c4416fa22817a54dc776f3973cf0b0e92aee209e667decc12e62de182b4f92f` — TransactionRepository
- [Project documents/ADR.md at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Project%20documents/ADR.md#L3460); SHA-256 `163d4d8f814bfd43a43b04e98f0396f9d6ac30d643871f1bc2568a49f493ed2c` — ADR-037
- [Project documents/UI Assets/LF-UI-2026-09-R1/ACCEPTANCE.md at adcf83f](https://github.com/vyom92/LedgerForge/blob/adcf83f52d309ddac18d95f0321d0c0f6120dd29/Project%20documents/UI%20Assets/LF-UI-2026-09-R1/ACCEPTANCE.md#L38); SHA-256 `e22c6acfe8e63a124f5f032dd9fb7e09a7102a3f25137e4acbb7a119845c03ab` — historical TX-01 to TX-10

## Unresolved decision or blocker

Import Preview alternatives remain proposals and require owner/design acceptance before selection. Remaining cross-screen R1 polish and ordinary interaction work need a refreshed SC-05A audit against accepted Sprint 91A; broad FW-P2-48/50 and FW-P2-46 work is not completed by that acceptance. Missing genuine zero-activity cases remain untested; no financial substitute may be manufactured.

## What would invalidate this conclusion

A future implementation must stop for a new approved boundary if it requires financial/source reinterpretation, persistence/schema changes, an ADR, a new design authority, or a new native interaction model. A screenshot alone cannot establish a financial-semantic defect.
